"""Generated by backend-skeleton (bskel handles emit) for feature {{FEATURE_ID}}.

Machine-owned companion to {{RESOURCE_TYPE}}Resolver -- every value here is derived directly from
the current contract/scan and is safe to regenerate on every `bskel handles emit` run, even when
the sibling resolver module has diverged (e.g. a hand-finished check_access()/patch_field()) and is
refusing to regenerate. See DECISIONS.md D-resolver-policy-split for why this is a separate module
rather than a section of the resolver module: a hand-edit to check_access()/patch_field() used to
also block CONTRACT_REF (feeding router.py's schema_drift check) from ever updating, since both
lived in one file and O2's conflict detection is file-level.

Nothing here is ever meant to be hand-edited -- to change one of these values, change the SOURCE it
was derived from (the feature contract, etc.) and re-run `bskel handles emit`. A hand-edit here will
be silently overwritten on the next run, by design.
"""
import uuid

type = "{{RESOURCE_TYPE}}"
CONTRACT_REF = "{{CONTRACT_REF}}"
FEATURE_UID = uuid.UUID("{{FEATURE_UID}}")
