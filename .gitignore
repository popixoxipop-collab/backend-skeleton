node_modules/
.DS_Store
*.log
# Ephemeral per-repo gate state this skill writes into a TARGET repo, not into this skill's own
# repo -- listed here only in case a manual test run leaves one behind under examples/ or test/.
.sbf/
# P3 (D-fixture-corpus): test/fixtures/java-compile/ IS actually built (unlike
# test/fixtures/java-spring/, which is scanned in place and never compiled) -- its Gradle wrapper
# is generated at CI/local-run time (scripts/java-compile-smoke.mjs), never committed, so a local
# run can't accidentally commit a wrapper jar or build output.
test/fixtures/java-compile/.gradle/
test/fixtures/java-compile/build/
test/fixtures/java-compile/gradlew
test/fixtures/java-compile/gradlew.bat
test/fixtures/java-compile/gradle/wrapper/
# A2 Phase 2 (D-java-ast-helper): the OPPOSITE of the fixture above -- this Gradle project's own
# wrapper (gradlew/gradle-wrapper.jar) IS committed (it ships inside the npm package, invoked at
# runtime by handles/providers/java-spring/ast-bridge.mjs), only its ephemeral build output isn't.
handles/providers/java-spring/ast-helper/.gradle/
handles/providers/java-spring/ast-helper/build/
