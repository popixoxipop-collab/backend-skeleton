{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:sbf:cross-feature-report:1",
  "title": "backend-skeleton cross-feature identity-collision report",
  "description": "Validates specs/<feature_id>/cross-feature-report.json, written by `bskel scan cross-feature-check`. Mirrors feature-contract.schema.json's own report-artifact precedent (always written, even when it finds nothing) -- see D-cross-feature-collision in DECISIONS.md. `findings` are NAME-identity collisions with other features only -- never a dependency-direction or FK claim (see lib/field-dependencies.mjs's own declared-dependency system for that).",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "feature_id", "generated_at", "findings"],
  "properties": {
    "schema": { "const": "sbf.cross-feature-report/1" },
    "feature_id": { "type": "string", "pattern": "^[0-9]{3}-[a-z0-9]+(-[a-z0-9]+)*$" },
    "generated_at": { "type": "string", "format": "date-time" },
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["signal", "identifier", "other_feature", "confidence"],
        "properties": {
          "signal": { "enum": ["resource_type", "table", "operation_id"] },
          "identifier": {
            "description": "The colliding string itself -- a resourceType/DTO className, a DB table name, or an operationId, copied verbatim from whichever side this finding was computed FROM (this feature's own scan report/contract).",
            "type": "string"
          },
          "other_feature": { "type": "string", "pattern": "^[0-9]{3}-[a-z0-9]+(-[a-z0-9]+)*$" },
          "confidence": {
            "description": "'high': the identifier was copied verbatim from a real source annotation on both sides (resource_type/operation_id always; table only when BOTH sides had an explicit @Table/__tablename__-equivalent). 'medium': at least one side's table name was INFERRED (an adapter's classname-lowercase fallback guess, not a real annotation) -- reported, but does not by itself block the gate.",
            "enum": ["high", "medium"]
          }
        }
      }
    }
  }
}
