// T06-owned nested entry. The shared root test glob intentionally does not discover this file;
// integration must provide an explicitly leased nested-T06 CI lane rather than a root bridge.
import './python-analyzer.cases.mjs';
