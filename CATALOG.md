# Enhancement idea catalog (Codex, 2026-08-15)

**Provenance**: produced by a Codex consultation (session `01a005a4-7d8f-7ac2-a4f6-255e967e76c7`,
`codex exec resume`) that read this repo's then-current state and proposed a full catalog of
follow-up work, organized by category with labels `A1-A5` (Architecture), `G1-G4`
(Generalization), `D1-D6` (DevEx), `S1-S6` (Gate/State mechanism), `P1-P4`
(Distribution/Adoption), `O1-O6` (Other).

**Recovery note (2026-08-16)**: this file did not exist until now -- the catalog was originally
only ever delivered as conversational Codex output, never written to a file in this repo. When a
later session tried to resume that same Codex thread (for what became A2's planning), the thread
itself was reported lost (`"No previous Codex task thread was found"`), so A2/A3 were planned from
a **fresh, independent** Codex consultation instead. **That fresh consultation's "A2"/"A3" labels
do NOT correspond to this original catalog's A2/A3** -- see the correspondence table below. The
catalog text itself was never actually gone: it survived in Codex CLI's own on-disk session
transcript (`~/.codex/sessions/2026/08/15/rollout-2026-08-15T22-37-41-01a005a4-....jsonl`),
independent of whether Claude's own thread-resume bookkeeping could reach it. Recovered by reading
that JSONL directly and extracting the `response_item` with `role: "assistant"` containing this
text, 2026-08-16.

**Label correspondence (original catalog vs. what was actually implemented under a same-looking label)**:

| Label (as implemented, this repo's history) | What was actually implemented | Matches this original catalog's same label? |
|---|---|---|
| A1 | OpenAPI operation reconciliation against a real generated document (`27fdc81`) | **Yes** -- matches A1 below almost exactly |
| A2 | Request body JSON Schema projection (`2b5ee8b`) | **No** -- this catalog's A2 is "staged Java analyzer" (regex -> balanced-token -> optional AST), never implemented |
| A3 | Response/error JSON Schema projection (`0205e3f`) | **No** -- this catalog's A3 is "approved patch-strategy generation" for `patchField()`, never implemented |
| A4 | (never implemented) | This catalog's A4 is "read-only database schema plane" -- still open |
| A5 | Contract completeness policy / waiver mechanism (`7513292`) | **Yes** -- matches A5 below closely |
| S1 | Unified gate definitions (`lib/gate-definitions.mjs`, `18d0838`) | **Yes** -- matches S1 below |
| S6 | Verify artifact-check fix (migration.sql disappearance now caught, `18d0838`) | **Yes** -- matches S6 below |

So of this catalog's 29 items (A1-A5, G1-G4, D1-D6, S1-S6, P1-P4, O1-O6), **4 are actually done**
(A1, A5, S1, S6) and **the rest (25 items) are still open**, including the real A2/A3/A4 (distinct
from what this repo's history calls "A2"/"A3"), all of S2-S5, and everything in G/D/P/O.

---

## Top 5 Priorities (Codex's own ranking, verbatim)

1. **Gate Engine v2: declarative dependencies, accurate invalidation, and history**

   Gates are the product's core differentiator, but current recomputers overstate their coverage: `handles` does not hash generated files, `stack` hashes only `.sbf/stack.json`, and `preflight` cannot notice a newly advanced remote tip. Move gate definitions, dependencies, input/output manifests, and verification policy into one registry before adding more phases.

2. **Make OpenAPI the typed contract plane**

   `buildContract()` currently derives only verb, path parameters, and a body-present boolean; this also causes the documented response/error gap and operation-correlation misses. A normalized OpenAPI 3.0/3.1 import and reconciliation layer delivers richer request, response, error, query, security, and cross-framework contracts, so it should precede both DB introspection and a heavier Java parser.

3. **Productionize handles before automating `patchField()`**

   `emitHandles()` unconditionally overwrites shared infrastructure and resolver files, while the generated controller does not enforce registry membership/revocation, field fetch always returns 501, and no code writes registry or snapshot records. First establish artifact ownership, a fail-closed authorization model, mint/register/snapshot lifecycle, and safe field reads; only then add assisted patch generation.

4. **Introduce a capability-based adapter IR and prove it with FastAPI**

   The Java scanner and downstream generators exchange an implicit, Spring-shaped object model, while `generic-grep` returns one `_generic` module with unknown verbs and no usable operation IDs. Define a versioned framework-neutral IR, then build FastAPI as the second first-class adapter because Python AST plus OpenAPI/Pydantic metadata provides a tractable validation of genuine portability.

5. **Turn the CLI into an installable onboarding product**

   The repository has no README, `package.json` is `"private": true`, there is no repo-level `bskel init`, and the hand-written flag parser produces inconsistent human/JSON errors. A packaged CLI with `init`, `status`, `next`, strict argument handling, installation smoke tests, and a minimal getting-started path is the shortest route from a personal skill to repeatable adoption.

**Status note (2026-08-16, corrected 2026-08-23)**: the original 2026-08-16 note below was itself
left stale for a week while #1/#3/#4/#5 were actually finished -- the same "boilerplate claim never
revisited after the code that closes it lands" class of bug this catalog's own G4/G5 entries had
(see D-typescript-express-provider's slice-4 correction in DECISIONS.md). As of `339e9eb`, **all
five priorities are closed**: #1 (Gate Engine v2) -- S1-S6 all `[IMPLEMENTED...]` below (declarative
dependency graph, content-manifest partial invalidation, preflight freshness, append-only history,
schema validation, and verification that cannot silently skip). #2 (OpenAPI typed contract plane)
-- delivered by A1-A3 (request/response/error schema projection + reconciliation), as this note
already said. #3 (productionize handles before automating `patchField()`) -- O1/O2/O4 all
`[IMPLEMENTED...]`; **O3 and O5 remain deliberately, conditionally deferred**, not "open" in the
TODO sense -- their own status notes below explain why (no evidence handles have ever been deployed
to production, re-confirmed as still true as of the G-track slices' own work). #4 (capability-based
adapter IR, proven with FastAPI) -- delivered and then generalized further than originally scoped:
G1 (the IR itself), G2 (FastAPI), G3 (generic-grep reconnaissance-only), G4 (framework-specific
codegen providers, java-spring + python-fastapi), and **G5** (a third stack, TypeScript/Express/
TypeORM, added after this catalog's own original scope -- see G5 below) are all `[IMPLEMENTED...]`.
#5 (installable onboarding product) -- D1-D6 and P1-P4/P3b all `[IMPLEMENTED...]`.

## Full Idea Catalog

## Architecture

### A1. OpenAPI import, reconciliation, and drift detection

- **What:** Add `bskel contract import-openapi --file …` and an explicitly opt-in `--url …`, plus `bskel contract diff`.
- **Why:** `contracts/emit.mjs::buildContract()` cannot derive query/header parameters, DTO fields, status codes, media types, security requirements, or response/error shapes. `scanners/index.mjs` also hardcodes `api_surface_source` without checking for a committed specification.
- **Scope/Effort:** **L** — new ingestion plane, normalized IR, diff classifier, and fixtures.
- **Concrete approach:** Normalize OpenAPI 3.0/3.1 operations into the contract IR, resolve internal `$ref`s with cycle protection, preserve source/hash provenance, and classify source-scan versus OpenAPI disagreements as breaking, additive, or metadata-only. Require a disposition for conflicting paths or operation IDs instead of silently choosing one source.
- **[IMPLEMENTED as A1, `27fdc81`]**

### A2. A staged Java analyzer rather than immediately replacing regex with JavaParser

- **What:** Improve the current scanner in two levels: a dependency-free balanced-token analyzer first, then an optional AST enhancer.
- **Why:** `extractController()` and `detectRequestBody()` depend on patterns such as `public\s+\S+`, nearby annotations, and fixed line structure. JavaParser Core would improve syntax handling, but DTO/service linking needs Symbol Solver and a bundled JVM helper, substantially weakening the lightweight Node CLI story.
- **Scope/Effort:** **M** for the balanced analyzer; **L** for an AST/symbol layer.
- **Concrete approach:** First mask strings/comments, balance annotation and method delimiters, record line spans, and add fixtures for generics, multiline annotations, records, package-private methods, and `@RequestMapping(method=…)`. Add an optional `java-spring-ast` capability later, invoked only when installed, for DTO fields, service signatures, validation, and security expressions; keep regex as the fallback.
- **[IMPLEMENTED (both phases) as A2, `c300065` (Phase 1) + `e032703` (Phase 2). Do not confuse with this repo's own "A2" (request body schema projection).**
  **Phase 1** — the dependency-free balanced-token analyzer. New `scanners/adapters/_java-spring-analyzer.mjs`: masks comment/string-literal content (delimiters kept for strings, so a real annotation's quoted value is never blanked — found live, not assumed, when a first draft blanked quotes too and broke every real `operationId` extraction, not just the phantom-comment case this item's own fixture already pinned), balances `(`/`)` and `<`/`>` (same algorithm `scanners/adapters/python-fastapi.mjs`'s own `matchBalancedParens()` already uses), and a general annotation-skip mechanism that fixes all 6 of P3's already-committed "known limitation (A2)" fixtures in `test/scan-fixture.test.mjs` at once (intervening annotations, comments, generics-with-spaces, same-line mappings, `@RequestMapping(method=...)` single-verb form, and the phantom-comment-operationId bug) plus adds `record`-class and package-private-method support (genuinely unsupported anywhere before this). `contracts/emit.mjs`'s `detectRequestBody()` — named directly alongside `extractController()` in this item's own Why — gets the same fix, confirmed live against a real, previously-unmatched generic-with-space return type. User-approved Tier 2: also consolidates `handles/providers/java-spring/plan.mjs`'s `methodMappingBoundaries()`, a literal duplicate of the scanner's old mapping regex, without touching D-security-7/8's own untouched logic (confirmed via unmodified passing tests). Found live via the real Team-IZ-Backend smoke test (not the fixture corpus alone): a real controller's `@RequestMapping(...) @RequiredArgsConstructor public class ...` shape needed the same annotation-skip fix applied to class-level detection too. Verified real-repo `scanJavaSpring()` output byte-identical before/after across 4 real modules, and all 9 generated Java files' sha256 hashes identical through a full `handles emit` run against a real isolated worktree, both with and without this change. Tests 568→594.
  **Phase 2** — the optional `java-spring-ast` capability, built exactly as originally specified (real JavaParser + Symbol Solver + a bundled JVM helper) after the user explicitly overrode a recommendation not to build it (the one concrete regex gap it closes hit 0/51 real DTO fields in the oracle repo). New `handles/providers/java-spring/ast-helper/` (a small, committed-wrapper Gradle project, `javaparser-symbol-solver-core:3.28.2`) resolves each DTO record field's real type via Symbol Solver (`type.resolve().describe()`) and each annotation's FQN via a hand-built import-map (NOT `AnnotationExpr.resolve()` — `javaparser/javaparser#1621` documents a `ClassCastException` there with no working fix). New `bskel handles plan --ast`: opt-in only, `classifyDtoFields()` itself stays synchronous and untouched, the helper runs alongside it and reports any disagreement as a purely informational `ast_disagreements` section — never auto-changes an approval or blocks anything. Scope, as the catalog's own text names four surfaces (DTO fields/service signatures/validation/security expressions): only DTO field type + annotation resolution ships this phase; service-signature and security/SpEL-expression resolution are explicitly deferred, not silently dropped. **Two real bugs found live, not assumed**: `StaticJavaParser`'s default language level doesn't support `record` declarations (`ParseProblemException`, fixed with an explicit `setLanguageLevel(JAVA_17)`); and building this item's own test fixture surfaced a genuine structural bug in Phase 1's shared `skipAnnotationsAndWhitespace()` (`_java-spring-analyzer.mjs`) — its `ANNOTATION_START_RE` didn't allow dots, so a fully-qualified annotation (`@jakarta.validation.constraints.NotNull`) was only half-consumed, corrupting the NEXT field's own parsed name/type, not merely failing to recognize the annotation's meaning (fixed: `/^@\w+/` → `/^@[\w.]+/`, verified via the full suite with no regressions). A third, packaging-only bug: `npm pack` shipped the AST helper's entire local `.gradle`/`build` output (11+MB) despite both being `.gitignore`d, because npm's `files`-directory walk doesn't apply a root-level ignore file's patterns to paths nested inside an already-allowlisted directory — fixed with a second, local `.npmignore` placed directly inside `ast-helper/` itself. Verified: real manual end-to-end runs on the development machine before any CLI/CI wiring; `test/handles-ast.test.mjs` (dependency-free `detectAstHelperAvailable()` unit coverage, always run) plus a new dedicated CI job `java-ast` (`scripts/java-ast-smoke.mjs`, a real clean-checkout Gradle build + both the plain-`@NotNull`-agrees and fully-qualified-`@NotNull`-disagrees cases) kept out of the fast default `npm test` path, matching this project's own established java-compile/java-integration/db-introspect precedent; a real `npm pack` + install round-trip (`test/package-install.test.mjs`) asserting the wrapper ships and the build cache does not. See D-java-ast-helper in DECISIONS.md.]**

### A3. Approved patch-strategy generation

- **What:** Generate a machine-readable patch strategy per resource and field instead of either guessing or always stopping at the same stub.
- **Why:** `ResourceResolverStub.java.tmpl` correctly documents three update conventions, but none of that knowledge is represented in the plan or contract. Capturing it explicitly would automate safe cases without erasing the permanent human-review boundary.
- **Scope/Effort:** **L**.
- **Concrete approach:** Analyze the update DTO, Bean Validation annotations, constructors/builders, and service update method; classify each pointer as `patch-wrapper`, `null-means-unchanged`, `fetch-merge-submit`, or `unsupported`. Add `bskel handles patch approve` to record the chosen strategy, generate only high-confidence mappings, and emit unit tests proving that validation and the existing service path are used.
- **[IMPLEMENTED as A3, `dfe81db` — Tier 2 scope (user-approved, see D-patch-strategy in DECISIONS.md): every field of the real update DTO the `bskel handles plan`-located PATCH/PUT endpoint accepts is classified into `patch-wrapper`/`null-means-unchanged`/`fetch-merge-submit`/`unsupported` (`handles/providers/java-spring/patch-strategy.mjs`, reusing A2 Phase 1's masking/balanced-delimiter analyzer). `bskel handles patch approve --resource --field --strategy --reason` (per-field, feature-scoped, fail-closed on a stale/mismatched strategy) gates real codegen for the two safe buckets ONLY — an explicit `jakarta.validation.Validator.validate()` call before the real service method, closing a validation-bypass risk found live (this codebase's `@Valid` is controller-boundary-only). `fetch-merge-submit`/`unsupported` are classified and explained per-field but never auto-generated, matching D-resolver-scope's original stub-over-guess precedent. **Two more real findings during implementation**: none of the 3 real update service methods checked actually have the plain (id, dto) 2-arg shape codegen assumes (each carries an extra scoping/auditing argument) — the same D-security-8-style param-count safety check now guards the update path too, and correctly blocks codegen there while still surfacing the classification; and the oracle repo's Spring Boot 4.1.0 ships Jackson 3 (`tools.jackson.databind`, not the classic package), detected from the Spring Boot Gradle plugin version rather than assumed. Verified: classifier output matches all 51 real fields across the oracle repo's 17 update DTOs exactly; the blocked-codegen path compiles clean in a real isolated worktree; the real codegen path (Validator/ObjectMapper/PatchField/ConstraintViolationException) compiles clean against a purpose-built synthetic fixture (no real 2-arg-shaped resource exists in the oracle repo today) now wired into CI's `java-compile-smoke`. Tests 613→650. See D-patch-strategy in DECISIONS.md.]**

### A4. Read-only database schema plane

- **What:** Add local-migration and live-PostgreSQL schema adapters.
- **Why:** `runScan({includeDb})` has no actual introspection implementation; setting it directly would merely suppress the "DB not scanned" unknown. Source entities cannot reveal live drift, RLS, triggers, views, functions, or all database constraints.
- **Scope/Effort:** **L**.
- **Concrete approach:** Scan repository migrations first, then offer explicit live introspection through a named environment-variable reference such as `--database-url-env TEST_DATABASE_URL`—never by reading `.env`. Use a read-only transaction over `pg_catalog`/`information_schema`, capture tables, columns, keys, indexes, policies and migration metadata, and gate on a normalized schema hash.
- **[IMPLEMENTED as A4, `b87ddf1` — both planes built, user-approved (AskUserQuestion): NO new pass/fail gate on the schema hash (the catalog's literal wording) — every other gate in this system is git/fs-derived with zero external-network dependency, and a gate requiring a live DB connection would be a new, CI-unavailable risk category this system has deliberately avoided everywhere else. Both planes surface as `bskel scan --db [--database-url-env <NAME>] [--schema public]` report content (`db_schema` + drift findings in `unknowns`, non-blocking) — the same "detect and warn, never gate" precedent A1 §7 already established. Plane A (`scanners/db/migrations.mjs`): Flyway/Liquibase migration-file detection + bounded regex extraction (good-enough, not a real SQL parser, same restraint as A2/G2's own analyzers) — unverifiable against the real oracle repo, which has zero migration files (confirmed live: schema is managed entirely in an external Supabase project). Plane C (`scanners/db/introspect.mjs`, new `pg` dependency — this project's first SQL driver): live `information_schema`/`pg_catalog` introspection inside a `BEGIN TRANSACTION READ ONLY` (confirmed live that Postgres itself rejects a write attempted inside it, not just "we didn't write one"), cross-checked against source entities for drift. **Two real bugs found live**: a `Promise.all()` over one `pg.Client` triggered a real deprecation warning (fixed to sequential awaits); a refused connection surfaces as a Node `AggregateError` with an EMPTY top-level `.message` (fixed with a dedicated `describeConnectionError()` fallback). Verified: real end-to-end runs against a real (disposable, local) Postgres, twice — once by hand, once wired permanently into CI as a new `db-introspect` job with a real service container (no hardcoded credential — `POSTGRES_HOST_AUTH_METHOD: trust`, after a secrets-scanning hook correctly flagged an earlier draft's throwaway password literal). Tests 666→687. See D-db-schema-plane in DECISIONS.md.]**

### A5. Contract completeness policy

- **What:** Distinguish "schema emitted" from "contract complete enough to trust."
- **Why:** `cmdContractEmit()` passes the contract gate even when `buildContract()` emits zero operations or warnings for unmatched/duplicate operation IDs. That permits an apparently successful workflow whose usable contract is empty.
- **Scope/Effort:** **M**.
- **Concrete approach:** Give warnings stable IDs and severities, add `complete`, `partial`, and `blocked` contract states, and write explicit waivers to a resolution file. Empty operation sets and skipped endpoints should block by default; low-risk metadata warnings may remain non-blocking.
- **[IMPLEMENTED as A5, `7513292`]**

### A6. OpenAPI export: render an emitted contract back out as a standalone document

- **What:** `bskel contract export --feature <id> [--out <path>] [--json] [--allow-unprefixed] [--status-codes range|literal]` — render an already-emitted, gate-passing feature contract as a standalone OpenAPI 3.1 document (stdout by default).
- **Why:** A1 built the import direction and A2/A3 made it load-bearing — `python-fastapi`, `typescript-express` and `javascript-express` all declare `api.operations: false` and depend on `--openapi-file` for any contract at all. There is no way back out today. The internal contract — feature-scoped, single-module, fully `$ref`-inlined (`inlineSchema()` already guarantees zero `$ref`) — is more useful than the source repo's own full OpenAPI document for three real consumers: a Swagger UI page scoped to one feature instead of the whole repo; a client generator that chokes on `$ref`; a mock server/gateway config for one feature's operations.
- **Scope/Effort:** **M** for the exporter + **S** for two importer fixes it forces.
- **Concrete approach:** Target OpenAPI 3.1 only. Never synthesize what the contract does not know — disclose every omission in prose (`info.description`) and machine-readably (`info.x-bskel-omitted`), and omit `security` entirely rather than emitting `security: []`. Map `responseSchema` → `responses["2XX"]` and `errorSchema` → `responses["default"]`, omitting `responses` when the contract has neither. Gate on the `contract` gate having passed; refuse a `blocked` contract, and refuse by default when the scan found a global path-prefix signal the contract's own paths don't reflect. Close the self-import hazard structurally: stamp `x-bskel-generated` on `info` and have `contract emit --openapi-file` refuse a document carrying it.
- **[IMPLEMENTED as A6, `27488d1` — full scope, plus two importer fixes and a structural guard the exporter forced. New `contracts/export.mjs` (pure — no I/O, no gate awareness, mirroring `buildContract()`/`reconcileModule()`); `cmdContractExport` owns every file and gate touch. **The design is dominated by one constraint: this is a LOSSY, NARROW projection that must never synthesize.** Measured against the real 148-operation Team-IZ-Backend oracle document, the contract carries NO query parameters (33/148 real operations have them, 99 total), NO header parameters (23/148), NO security (148/148 real operations require bearer auth), no summary/description/tags, no per-status responses (22% of real operations don't return 200), and nothing for a non-JSON request body (5 real multipart operations). All disclosed twice, and phrased as "this projection cannot represent X" rather than "the real API has X we dropped" — the same "describe the method, not an unchecked fact about the repo" discipline A1 §7 applied to `api_surface_source`. **`security` decided the general rule**: emitting `security: []` is spec-legal (confirmed by executing the real meta-schema against a document containing it) and is also a positive false claim that no auth is required — so schema validity cannot be what decides the output, truthfulness has to; the key is OMITTED, which means "unspecified", which is true. Same rule kills `{type:'object'}` for a body of unknown shape: an operation that takes a body with no projected schema gets `content: {'application/json': {}}` — a media-type entry with no schema, the only way to say "there is a JSON body here whose shape this contract does not know". **3.0 output is excluded with citations, not deferred** — verified by executing the official 3.0 meta-schema: its `Operation` has `required: ["responses"]` (forcing a synthesized response for an operation the contract knows nothing about), it types `exclusiveMinimum`/`exclusiveMaximum` as BOOLEANS while `COPIED_KEYWORDS` copies them as the numbers a 3.1 source used (silently inverting their meaning), and its Schema Object has no `const` and no `"null"` type, both of which a projected schema can legitimately contain. **The naive round trip was rejected on a traced hazard, not built**: a `drift` operation sits in `contract.operations` at the scan's own uncorrected verb/path, so exporting and re-importing makes `computeDelta()` agree and silently reclassifies an ERROR as `matched`; `missing` behaves the same, and `ambiguous` comes back as a different code entirely, breaking any recorded waiver. What ships is the narrow, provable version — for a `complete` contract, export → re-import against the SAME scan report reproduces `operations` exactly ("convergence after one step", since `format:'uuid'` becomes a bare-UUID `pattern` on import per D-security-2). **The self-import guard is proven load-bearing, not decorative**: a drifted contract's export is refused at the existing exit-14 path, and the SAME document with only the `x-bskel-generated` marker stripped genuinely does launder the drift into `matched` with the ERROR gone and completeness flipping `partial`→`complete`. **Two importer fixes, real general capability gains**: `SUCCESS_STATUS_RE`/`ERROR_STATUS_RE` widened to accept OpenAPI's own range keys (`/^2(?:[0-9]{2}|XX)$/`, `/^[45](?:[0-9]{2}|XX)$/`, matching the 3.1 meta-schema's own `^[1-5](?:[0-9]{2}|XX)$`), and `default` folded into the ERROR side only — folding it into success would let an error shape satisfy success validation, folding it into error only ever widens a union `anyOf` already tolerates. Both confirmed to FAIL against the pre-fix code before the tests were kept (`2XX`+`4XX` projected neither schema; `200`+`default` lost the error one; `default` alone yielded nothing), and a document written with range keys previously lost every schema SILENTLY, since "no matching status" is correctly not a failure anywhere. **Three real findings by execution**: OpenAPI 3.1 REQUIRES `description` on a Response Object (not anticipated in planning — resolved by making the two strings describe the projection, not the API); Ajv 8.20.0 mis-resolves the official 3.1 meta-schema's own `{"$dynamicRef": "#meta"}` nodes, applying the ENCLOSING subschema to the referenced value (reproduced against a 6-line synthetic schema to confirm it is Ajv, not a usage error — handled by a documented, spec-faithful rewrite to `$ref: '#/$defs/schema'` in the TEST only, leaving the vendored file byte-identical to the published one); and `security: []` validating cleanly, which is the finding that decided the omit-never-fabricate rule. Deliberately unchanged, confirmed not skipped: `contracts/completeness.mjs` (no new warning codes — export reads A5's verdict, adds nothing to it), `lib/gate-definitions.mjs` (no new gate/token input — an export is a derived OUTPUT, and hashing it in would stale the gate on every re-export, backwards; same reasoning D-handles-ownership used), `lib/verify.mjs`, and `.github/workflows/ci.yml` (pure Node, runs inside plain `npm test`). The contract-flow test harness moved to `test/_contract-fixture.mjs` so `contract-cli`/`contract-export` share one fixture; all 43 pre-existing tests pass unmodified across the move. Exactly ONE pre-existing test needed modifying — `test/contract-openapi.test.mjs` had explicitly PINNED the old "`default` contributes to neither bucket" behavior, so that was deliberate at the time rather than an oversight, and is retargeted (keeping its success half, which is the load-bearing one) rather than quietly rewritten. `npm test` 771 → **792**. See D-openapi-export in DECISIONS.md.]**

## Generalization

### G1. Versioned adapter IR and capability negotiation

- **What:** Define a JSON-schema-validated `ProjectProfile` and discovery IR for modules, operations, DTOs, resources, security policies, persistence, and update strategies.
- **Why:** `runScan()` hardcodes two adapters, while contracts and handles rely on undocumented fields such as `controller.file`, `ep.method`, and the `domain/<module>` layout.
- **Scope/Effort:** **M**.
- **Concrete approach:** Give each adapter `detect`, `scan`, `capabilities`, and `diagnostics` methods. Commands should require capabilities such as `api.operations`, `resource.fetch`, or `codegen.handles`, producing an actionable unsupported-capability result rather than failing later on Spring-specific assumptions.
- **[IMPLEMENTED as G1, `ae7b10e` — zero-registration adapter registry (`scanners/registry.mjs`, mirrors `stack/apply.mjs`'s `listCatalogChoices()`/D7): each `scanners/adapters/<id>.mjs` exports an `sbf.adapter/1`-contract `adapter` object (`id` must equal its filename, `specificity` for arbitration, `confidence`, `capabilities`, `detect`/`scan`/`diagnostics`), validated against new `schemas/adapter.schema.json`, discovered by directory scan + dynamic `import()` — adding an adapter touches exactly one file, adding a capability touches zero adapter files (fail-closed: undeclared = false). `scanners/index.mjs`'s hardcoded two-branch dispatch replaced with specificity-based arbitration that throws (naming every candidate) on a genuine tie, mirroring O6's `detectBasePackage()` refusal. Also fixed a real bug found while researching this: `schemas/scan-report.schema.json` was missing the `path_prefix_signals` property under `additionalProperties:false`, so every real scan report ever produced failed its own schema — proof it was never actually validated. New capability-checking layer (`scanners/capabilities.mjs`, `bin/bskel.mjs`'s `requireCapabilitiesOrExit()`) intercepts in `contract emit`/`handles plan`/`handles emit` *before* any Java-specific codegen runs, with a new exit 17 that names the missing capability and the adapter, instead of the previous confusing fall-through to `detectBasePackageOrExit`'s "is this a Spring Boot project?" for a non-Spring repo — a path that had zero test coverage before this. `bskel doctor` now lists every installed adapter's specificity/capabilities and whether it detects the current repo. Explicitly did NOT build G2 (a real FastAPI adapter) or G4 (splitting handle codegen into swappable providers) — `codegen.handles: false` for `generic-grep` is the correct, honest answer, not a limitation to paper over. Verified byte-identical against Team-IZ-Backend (scan/contract/handles output, generated Java files, and exit codes all unchanged pre/post-refactor) and the zero-registration claim demonstrated live (copied a fixture adapter file into `scanners/adapters/`, `bskel doctor` picked it up immediately, deleted it, no other file touched). See D-adapter-registry in DECISIONS.md.]**

### G2. FastAPI as the second first-class adapter

- **What:** Build `python-fastapi`, not a more elaborate generic grep, as the portability proof.
- **Why:** FastAPI exposes routes and Pydantic schemas through OpenAPI and has statically inspectable decorators via Python's standard `ast` module. This gives better coverage per unit of effort than Express router composition.
- **Scope/Effort:** **L**.
- **Concrete approach:** Use OpenAPI for operation/schema truth and Python AST for file/module provenance, dependency declarations, service collisions, and repository patterns. Never import or execute the target application during a source scan; runtime OpenAPI retrieval must be an explicit user action.
- **[IMPLEMENTED as G2 (1st slice), `21bea3c` — new `scanners/adapters/python-fastapi.mjs` (specificity 90, zero-registration), verified against a real cloned FastAPI oracle (`fastapi/full-stack-fastapi-template`, official reference repo). Two findings drove the design, both reproduced by execution: (1) the pre-G2 baseline reported `greenfield` (false negative) for the real oracle — `generic-grep` found all 23 routes but collapsed them into one unscored `_generic` module; (2) `--openapi-file` alone was NOT enough to adopt real operations — `contracts/openapi.mjs`'s prefix inference only builds anchors from endpoints that already have an operationId, which a FastAPI module has zero of, so `--path-prefix` is also required (both verified: 0 adopted without it, all real endpoints adopted with it). Extraction is pure Node regex, NOT Python `ast`/interpreter shell-out — this deliberately overrides this item's own "Python AST module" text, on the same reasoning A2 used to reject a JVM helper for Java (this CLI's only external binary dependency stays `rg`); verified 100% extraction fidelity (5/5 real Items + 10/10 real Users endpoints, correct verb/path/function-name, real entity table/idField cross-checked against the oracle's own Alembic migration) with zero accuracy loss. Capabilities declared honestly: `api.operations`/`api.request-shape` false (FastAPI generates operation ids at runtime; this project's request-body detection is Java-only), `resource.fetch` true (table/idField genuinely extracted), `codegen.handles` false (no Python provider — G4's job, `handles/*.mjs` completely untouched). New `CAPABILITY_SATISFIERS` in `scanners/capabilities.mjs` (data keyed by capability, not by adapter) lets `--openapi-file` bypass the `api.operations` gate at `contract emit` for ANY adapter with this honest weakness — including `generic-grep`, accepted rather than special-cased, tested to still end honestly `blocked`. `handles plan`/`emit` against a FastAPI-scanned feature still correctly exit 17 on `codegen.handles`, never attempting Java codegen. Tests 345 → 356. See D-fastapi-adapter in DECISIONS.md. G4 (Python codegen provider) explicitly NOT built.]**

### G3. Keep `generic-grep` as reconnaissance only

- **What:** Improve its evidence, but stop treating it as contract-grade.
- **Why:** `generic-grep.mjs` collapses every route into `_generic`, uses `verb: "?"` for some frameworks, omits method names, and provides no operation IDs. The current workflow can nevertheless resolve the scan and pass an empty contract.
- **Scope/Effort:** **S–M**.
- **Concrete approach:** Add repo-relative file/line evidence, framework confidence, route prefixes, and verb extraction where cheap. Require `--accept-low-confidence` or a forced gate before using it downstream; otherwise its role is to answer "something related exists."
- **[IMPLEMENTED as G3, `001b75f` — Codex's original concern ("the workflow can resolve the scan and pass an empty contract") turned out to be stale at the contract stage: `classifyContract()` already makes a zero-operation contract unconditionally `blocked`, and every generic-grep endpoint has `operationId: null` by construction, so it always hits that path. The real gap was one level earlier: `bin/bskel.mjs` never referenced `confidence`/`adapter` anywhere (confirmed by grep across the whole file) — a low-confidence scan's verdict (including `greenfield`, which auto-passes the `scan` gate) was acted on with zero confidence-awareness. New `--accept-low-confidence` flag on `bskel scan --feature <id>`: blocks (new exit code 16) before writing the scan report or touching the `scan` gate at all when `confidence === 'low'`, regardless of verdict; ad-hoc mode (`--terms` without `--feature`) is unaffected, already read-only and gate-untouched. **Two real bugs found live while adding the cheap evidence/prefix half of this item, neither in the original catalog text**: `scanGenericGrep()` turned every matched ROUTE into its own fake controller, so `scoreModule()`'s className-match rule got counted once per route instead of once per file — a file with 50 Express routes could score 300 on a term alone, unrelated to real module relevance — fixed by grouping routes by source file before scoring, with `basePath` computed as the real shared path prefix across that file's routes. And the express-router/fastapi verb regexes shared the bare `router.get(` substring with no anchor, so every FastAPI route matched both patterns and was silently double-counted — invisible before the grouping fix, obvious as a duplicated pair afterward — fixed with a `(?<!@)` negative lookbehind. Also added as genuinely cheap: real captured HTTP verbs (3 of 4 route patterns already captured it and discarded it) and file/line evidence. Deliberately NOT done: per-route/per-framework confidence tiers (no real non-Java target-repo corpus to calibrate against — Data-First Numerics) and Flask `methods=[...]` parsing (needs real argument parsing, not a regex tweak). New `test/generic-grep-cli.test.mjs` (built from scratch — no non-Java fixture-building helper existed anywhere in `test/` before this). See D-generic-grep-reconnaissance in DECISIONS.md.]**

### G4. Framework-specific codegen providers

- **What:** Separate handle semantics from Spring code generation.
- **Why:** `cmdHandlesPlan()` and `cmdHandlesEmit()` always detect a Java base package and write Java/Spring templates, even if a different scanner produced the contract.
- **Scope/Effort:** **L**.
- **Concrete approach:** Produce a framework-neutral handle manifest first, then dispatch to `providers/java-spring`, `providers/python-fastapi`, or `providers/typescript-express`. Each provider declares required capabilities and output ownership rules.
- **[IMPLEMENTED (1st slice), `627c214` — `handles/registry.mjs` (zero-registration provider dispatch, mirrors G1's `scanners/registry.mjs` exactly, `sbf.handles-provider/1` contract + `schemas/handles-provider.schema.json`), java-spring extracted behind that same interface into `handles/providers/java-spring/` with its safety-critical write/conflict/manifest/orphan logic pulled into a shared `handles/_engine.mjs` (27 pre-existing handles tests pass with exactly ONE line changed — an import path, nothing else, confirming the extraction preserved behavior rather than coincidentally passing), and a REAL second provider: `handles/providers/python-fastapi/` (codec ported from `handles/codec.mjs` and EXECUTED round-trip-verified against it, both directions, positive and negative parity — the first time this repo's "byte-identical" codec claim has ever been verified by running code rather than asserted in a comment; in-process registry, `fetch`/`to_public` really wired to `session.get(...)`/`<Entity>Public.model_validate(...)`, `check_access`/`patch_field` always fail-closed stubs, GET+PATCH router, deliberately no migration/`recover()`). `COMMAND_CAPABILITIES` narrowed to dispatch-only (`codegen.handles`); each provider now declares its own `requiresCapabilities`, producing an unfakeable biconditional (`codegen.handles === true` ⟺ a provider is loaded for that adapter id) pinned as a dedicated regression against every real shipped adapter. `python-fastapi`'s own `codegen.handles` flips to `true` — G2's "no Python provider exists" caveat is now false. Real-oracle verified (`fastapi/full-stack-fastapi-template`, throwaway branch): BOTH `items` and `users` modules generate a resolver, and BOTH correctly fail closed on `check_access()` — the oracle's real authorization logic lives inside route bodies, not decorators, which static scanning genuinely cannot see; the `User` resolver projects through `UserPublic` and never references the table's own `hashed_password` column (confirmed by grep across every generated file). Every generated file syntax-validated via `python3 -c "ast.parse(...)"`; re-emit is idempotent; a hand-edited resolver blocks re-emit at exit 15, byte-for-byte untouched. Tests 356 → 386. Explicitly NOT built: `recover()`/its tables for Python (dead code even on the Java side — O4 still unimplemented), field/object-level handle fetch (`kind=f`/`o`, matching Java's own current limit), a `--provider` override flag (no real N:1 case observed), `src/`-layout support, automatic router wiring into the app (two lines a human adds by hand). **Update, `3339ab1`**: the matching JS↔Java codec "byte-identical" claim — left open above as a named EXIT, not silently claimed closed by association — is now closed too: `test/handles-java-codec.test.mjs` executes the rendered `HandleCodec.java.tmpl` against a real `javac`/`java` (a line-protocol driver, no JSON dependency, mirroring the Python test's own "mandatory, not skippable" design; `actions/setup-java` added to the `test`/`macos` CI jobs since they run plain `npm test`), both directions, positive and negative parity, plus a direct confirmation that `Base64.getUrlDecoder().decode()` really does throw on invalid-charset input (mirroring the Python test's own confirmation of the opposite fact for Python). **Update, `2386368`**: the other line from "Explicitly NOT built" above — "`recover()`/its tables for Python (dead code even on the Java side — O4 still unimplemented)" — is now closed too, its own precondition ("if O4 ever gets implemented") having held: new `tables.py`/`handle_service.py`/`record_snapshot.py` (SQLModel `HandleRegistry`/`HandleSnapshot`, same DDL as Java's own migration.sql; a real `GET /handles/{handle}/recover` with the full D-security-9 cross-check preserved; a real opt-in `@record_snapshot` decorator — ONE file, not Java's two, since Python decorators natively are the interception mechanism); real field-level fetch (new `resolve_json_pointer` in `codec.py.tmpl`, with a `_MISSING` sentinel a literal JS port would have gotten wrong for a present-but-null field). `HandleSnapshot.payload` is a native `JSONB` column (never a manually (de)serialized string), making Java's own double-encoding bug class structurally impossible here, not merely avoided. A real functional test caught a real design bug before it shipped: the decorator's first draft broke every `redact` pointer for the common single-DTO case (wrapped the payload in `{param_name: value}` instead of unwrapping the sole argument, matching Java's own `requestPayload` behavior). Verified against a real, disposable Postgres via FastAPI's own `TestClient` (new `scripts/python-integration-smoke.mjs`, CI job `python-integration`) — the same four real invariants O4's own Java verification proved (full lifecycle, schema_drift, field-fetch, persistence-layer redaction), at meaningfully lower cost than Java's own live-server requirement since nothing here needs what only a bound port proves. **Correction (slice 4, docs-only)**: the "Explicitly NOT built" line above naming "field/object-level handle fetch (`kind=f`/`o`, matching Java's own current limit)" was true when the 1st slice shipped but was never revisited after the `2386368` update above (Python) and Java's own O4 (`a816312`) each separately closed the `kind=f` GET-fetch half of it — confirmed by reading `HandleController.java.tmpl`/`router.py.tmpl` directly. `kind='o'` remains genuinely undifferentiated from `kind='r'` at the codec level (an unscoped design question, not a fetch-support gap) — see D-handles-providers in DECISIONS.md for the full correction. See D-handles-providers in DECISIONS.md.]**

### G5. TypeScript / Express / TypeORM as the third scanner adapter + handles provider

- **What:** Build `typescript-express`, the third provider G4's own "Concrete approach" text already names as hypothetical.
- **Why:** G4 shipped two providers (java-spring, python-fastapi); a third, structurally different ecosystem is the strongest remaining proof this project's adapter/provider split generalizes, not just doubles.
- **Scope/Effort:** **L**.
- **Concrete approach:** Detect via `package.json`'s `express` dependency + source-confirmed `Router()` usage; extract routes via balanced-paren scanning and a mount-tree walk (Express has no local base-path syntax); target TypeORM's current `DataSource` API; require a literal `select: [...]` allow-list as the codegen precondition, the same role `<Entity>Public` plays for Python.
- **[IMPLEMENTED as G5, `7584ddf` — new `scanners/adapters/typescript-express.mjs` (specificity 85, zero-registration) + `handles/providers/typescript-express.mjs` + `typescript-express/{plan.mjs,emit.mjs,templates/*}`. Unlike G2 (verified against `fastapi/full-stack-fastapi-template`, the official FastAPI-author-maintained reference), **no framework-maintained Express reference exists** — confirmed by real research (WebSearch + `gh api` star/fork/maintenance comparison across 5 candidates) before writing any code; verified instead against the best-validated real community boilerplate, `mkosir/typeorm-express-typescript` (461 stars/149 forks, not a fork itself, cloned and read, not skimmed from memory). Seven real findings from that reading drove the design, none mechanically ported: (1) the oracle's own TypeORM API is stale (`^0.2.45`, `getRepository()`) — this provider deliberately targets the CURRENT `DataSource` API instead, so running against the oracle *as it exists today* generates zero resolvers, an honest reflection of staleness, not a bug; (2) no `<Entity>Public`-equivalent convention exists in this ecosystem's real code — the precondition is instead a literal `select: [...]` allow-list found in the fetch handler's own file, reusing what the oracle's real code actually does; (3) middleware arrays nest parens (`checkRole(['ADMIN'], true)` inside `router.get(path, [mw], handler)`), needing balanced-paren scanning, not a naive regex; (4) route registration and handler logic live in separate files linked by one barrel-re-export hop; (5) **no local base path exists anywhere** — a route's real absolute path only exists as the join of `router.use('/literal', subRouter)` mount edges walked from a graph root to a leaf, the single most novel piece of this adapter; (6) `checkRole(...)` is project-specific, not a framework mechanism, so deliberately NOT mined as an authority signal; (7) Express 4 (the oracle's pinned version) does not auto-catch a rejected async-handler Promise, so every generated route wraps its body in an explicit try/catch. A real, structural (not TypeScript-specific) constraint found live via a genuine `tsc --noEmit` type error, not assumed: this whole handle system's token format can only address a UUID-shaped resource identifier, and TypeORM's bare `@PrimaryGeneratedColumn()` defaults to an auto-incrementing integer — the real oracle's own `User` entity uses exactly that form — fixed by adding `idFieldIsUuid` detection and gating codegen on it, with an honest refusal note. `ResourceResolver` deliberately carries no dataSource/session parameter (TypeORM's `DataSource` is an app-wide singleton, unlike SQLAlchemy's per-request `Session`) — a genuine architectural simplification, not an arbitrary deviation from python-fastapi's own interface shape. `codec.ts.tmpl` is a self-contained port (zero imports beyond `node:crypto`, never importing this CLI's own `handles/codec.mjs` into a generated file), needing **no** `_MISSING`-sentinel workaround unlike the Python port (TypeScript/JavaScript already distinguishes `undefined` from `null`, confirmed live). Five real bugs found and fixed while building this: an unpinned `npm install typescript` installs TS 7.0.2 (a breaking preview compiler), not mainstream 5.x; sequential separate `npm install --no-save <pkg>` calls silently pruned earlier installs; `req.params.handle` is really typed `string | string[]`, needing explicit guards; `strictPropertyInitialization` needed TypeScript's definite-assignment assertion (`id!: string`) in the fixture, which then broke the idField regex until fixed to tolerate the `!`; and `bskel scan`'s own cached report doesn't auto-regenerate when scanner code changes, a real reusable debugging trap for this tool's scan-then-plan pipeline. 33 net new tests (`test/handles-typescript-codec.test.mjs` x9 — mandatory inside plain `npm test`, no external toolchain needed beyond Node itself, unlike the Java/Python codec tests; `test/typescript-express-cli.test.mjs` x6; `test/typescript-express-handles.test.mjs` x13, including a real fixture-authoring bug found and fixed live — an explanatory comment reading `select: [...]` accidentally satisfied the very regex the fixture meant to demonstrate the absence of) plus new `scripts/typescript-typecheck-smoke.mjs` + CI job `typescript-compile` (a real `npx tsc --noEmit` against emitted code, proving generated code actually type-checks — the codec test alone proves runtime parity, not that TypeScript's own compiler accepts it). Tests 709 → 742. Explicitly NOT built: `recover()`/its tables (matches java-spring/python-fastapi's own pre-follow-up state), a `--provider` override flag (structurally not just unobserved — see D-typescript-express-provider's EXCLUDED section), automatic router wiring into the app. **Correction (slice 4, docs-only)**: "field/object-level handle fetch" was listed here too, but was false the moment this bullet was written — `router.ts.tmpl`'s GET handler (shipped in this same commit) already walks a JSON Pointer via `resolveJsonPointer` whenever one is present, the same generic pointer-walk java-spring/python-fastapi already ship; only `kind='o'` (object) has no distinct runtime behavior, since `handles/codec.mjs` never lets it carry a pointer — an unscoped codec-level design question, not a missing fetch feature. A new runtime test closing this provider's own coverage gap (it previously had none exercising the pointer-walk, unlike java-spring/python-fastapi) was added in the same slice-4 pass. **This item's real-world verification confidence is permanently lower than G2's or G4's** — grounded in one real, hand-read community oracle rather than a framework-maintained reference stack, named as a standing gap, not a to-do. See D-typescript-express-provider in DECISIONS.md.]**

### G6. Plain-JavaScript, non-ORM Express (Serverless Framework/Lambda-deployed) as a fourth handles-codegen target

- **What:** Detect and (if safely feasible) generate handles for Express apps written in plain JavaScript, not TypeScript, using a raw SQL driver (`mysql2`/`mariadb`) directly instead of an ORM, commonly deployed to AWS Lambda via the Serverless Framework's `serverless-http` wrapper.
- **Why:** Found live while investigating a real, unrelated Lambda cold-start comparison question — `sayyou-backend-staging-main` (AWS Lambda, `nodejs20.x`, examined directly via `aws lambda get-function`) is a completely standard `express.Router()` app (`src/app.js`: `express()`, `route.use('/user', userRoute)`, etc. — the exact idiom G5 already scans for), thinly wrapped by `src/lambda.js`'s one-line `ServerlessHttp(app)(event, context)`. It is entirely invisible to `backend-skeleton` today for two independent reasons, both confirmed by reading the real shipped code, not assumed: (1) `scanners/adapters/typescript-express.mjs`'s `detectTypeScriptExpressRoot()` restricts its ripgrep source search to `-g '*.ts'` only — this repo has zero `.ts` files (`"type": "module"` ESM, but no `typescript` devDependency at all), so `detect()` unconditionally returns `null` and the repo falls through to `generic-grep`. (2) Even a hypothetical TypeScript port would still fail G5's own handles-codegen precondition: `handles/providers/typescript-express/plan.mjs`'s `findDataSource()` requires a TypeORM `DataSource` instance somewhere in the project — this app has no ORM anywhere, `mysql2`/`mariadb` are called directly from controller code.
- **Scope/Effort:** **L, genuinely uncertain — do not over-commit before Phase 1's own investigation reports back.**
- **Concrete approach:** Two independently-shippable phases, matching this project's own G2-then-G4 precedent (scanner-only first, codegen only once a real safe convention exists) — do not force both into one slice if Phase 2 turns out unsafe.
  - **Phase 1 (scanner, likely mechanical)**: generalize (or fork, if the two need to diverge) the existing `typescript-express` adapter to also search `.js` files under `"type": "module"` ESM — same `Router()`/`router.<verb>()` idiom, same balanced-paren middleware handling, same mount-tree walk (a `.js`-authored Express app has no reason to structure routing any differently than a `.ts`-authored one). Declare `codegen.handles: false` honestly if Phase 2 isn't shipped yet or isn't safe — matches G2's own original 1st-slice precedent (a real scanner with zero codegen is a legitimate, shippable state, not a half-finished one).
  - **Phase 2 (handles codegen, genuinely uncertain — investigate for real before designing)**: without an ORM's own decorator/column metadata, there is no structurally reliable way today to discover a resource's real table name, primary-key field, or a safe column allow-list from raw `mysql2`/`mariadb` call sites (arbitrary SQL strings, no `@Entity`/`@PrimaryGeneratedColumn`/`select: [...]` anchor to scan for). Read real target code (this app's own `src/controllers/**/*.js` — SQL string literals, `pool.query(...)` shapes, any migration/schema files like the `database.sql`/`migrations/*.sql` this specific app ships) before assuming any particular heuristic (e.g. parsing `SELECT ... FROM <table> WHERE id = ?`-shaped literals) is reliable enough to safely gate codegen on. If no safe convention is found, the correct, honest outcome is to ship Phase 1 only and explicitly not build Phase 2 — a false promise of safe resolver generation over raw SQL is a real security risk (the exact class of mistake `D-security-8`/`D-resolver-scope` already exist to prevent), not a shortcut worth taking.
- **[IMPLEMENTED as G6 Phase 1, `2c5e94e` — Phase 2 explicitly NOT built, see `D-javascript-express-adapter`'s EXCLUDED section in DECISIONS.md.]** Shipped as a new `javascript-express` adapter (specificity 80, `confidence: high`, all four capabilities honestly `false`), **not** a generalization of `typescript-express` — the fork was decided from real code, not preemptively, on three findings: capabilities are per-adapter booleans, so a merged adapter would have to declare `codegen.handles` either `true` (a lie for plain JS, whose provider path requires a `tsconfig.json` and would throw rather than refuse cleanly) or `false` (a silent regression of G5's working codegen); `import express from 'express'` + `express.Router()` is the dominant plain-JS idiom and G5's detect() requires a NAMED `Router` import; and the router variable is frequently not called `router` (the real target app's own entry file declares `const route = express.Router()`, which G5 hardcodes past). The genuinely novel mechanism is a mount graph over **(file, router-variable)** nodes rather than G5's file nodes — the real app's global `/api` prefix lives on an INTRA-FILE edge from the `express()` application to a locally-declared Router (`app.use('/api', route)`), with no import involved, which the file-to-file model cannot represent at all; the e2e test resolves `/api/user/:userUid` and separately asserts the leaf file contains no `/api` anywhere. ESM scoping is Node's own rule applied as written (`.mjs` always; `.js` only under `"type": "module"`), so CommonJS falls out of scope by construction rather than by an exclusion check — tested in both directions. **Phase 2 was investigated with a real fixture and executed probes before being designed, and then rejected on measurement, not schedule**: an extraction probe implementing the most generous plausible `SELECT ... FROM <table> WHERE <pk> = ?` heuristic extracts **0 of 5** SELECT literals cleanly against realistic raw-`mysql2` controller code (multi-table JOIN, `o.*` star projection, runtime-concatenated SQL, statements living in a separate constants module, and two handlers projecting different column sets from the same table); the single friendliest near-miss still silently drops a `AND deleted_at IS NULL` predicate, which an executed `node:sqlite` check showed makes the reconstructed query return **1 row where the app's own query returns 0** — a handle resolver serving a soft-deleted account the application itself refuses to serve, the same silently-dropped-scoping-predicate class `D-security-8` exists to prevent, here with no compiler to catch it; and generating a fresh SQL string is outside what `D-resolver-scope` permits a resolver to be at all (every shipped provider delegates to something already tested — a raw-SQL stack has nothing to delegate to). A schema-file rescue (the fixture ships a realistic `database.sql`) answers the UUID-vs-integer primary-key question but nothing else, and is a per-app habit with no runtime enforcement, unlike Alembic or TypeORM metadata. Six real bugs found and fixed along the way, all reproduced live, and five of them share one signature worth naming — the scan still SUCCEEDS and still reports routes, just with silently wrong paths, a silently missing global prefix, or a silently empty result, never an error. First, a **comment-masking bug** where an unmasked `import ... from 'express'` regex matched inside a header comment and ran across the newline into the real statement, silently collapsing the whole mount graph while every route still looked correctly scanned (fixed with a new `maskJsComments()`, the JS counterpart of A2 Phase 1's `maskNonCode()`, and **deliberately applied to `typescript-express.mjs` too** — the same exposure meant a commented-out `// router.get(...)` was being reported as a LIVE endpoint there; the regression test was confirmed to fail against the pre-fix code before being kept). Second, a member-call regex anchored to the wrong end that classified every `express.Router()` as a bare `Router()`. Third, a `rg --files`-vs-`path.resolve()` path-normalization mismatch that drops every cross-file mount edge under a relative `repoRoot` (latent today, since real callers pass an absolute one). Fourth: every `Router()` check in the codebase required literally empty parens, so a router declared `Router({ mergeParams: true })` — completely ordinary Express — made G5 skip the whole file and left G6 unable to recognize the variable as a router at all; widened to `/\bRouter\s*\(/` in all three places. Fifth: the import-clause parser matched a whole statement forward, which in semicolon-less (standard.js-style) ESM runs straight through the PREVIOUS import and loses the default binding name — and with it every `express()` application and any global prefix mounted on one; rewritten to anchor on `from 'express'` and scan backward, with a strict clause-shape check that refuses rather than guesses, which also fixed multi-line clauses for free. Sixth: `maskJsComments()` itself tracked only strings and comments, so a regex literal containing an odd number of quotes (`/'/g`) opened a phantom string running to the next quote anywhere later in the file — reintroducing the very phantom-route bug it exists to prevent; fixed by tracking regex literals with the standard previous-significant-character heuristic, a deliberately narrow preceder set, and an end-of-line bail so a misjudged division can damage at most one line. `npm test` 743 → **771** (28 new), every pre-existing test unmodified. Honest limitation, named not hidden: unlike G2 (verified against the official FastAPI reference stack) and G5 (one hand-read community boilerplate), this item has **no real-world oracle at all** — the real target repository was deliberately not touched, and the committed synthetic fixture carries all of the regression weight.

## DevEx

### D1. `status`, `next`, and guided workflow commands

- **What:** Add `bskel status --feature`, `bskel next`, and optionally `bskel next --execute`.
- **Why:** `gate show` exposes raw state and `verify` is primarily an end-of-flow aggregate; neither explains the next blocking action across repo- and feature-scoped gates.
- **Scope/Effort:** **M**.
- **Concrete approach:** Evaluate the dependency graph, show stale reasons and missing artifacts, then print one copy-pasteable next command. JSON output should include `blocked_by`, `next_actions`, and whether each action is read-only or mutating.
- **[IMPLEMENTED as D1, `b96cc56` — `bskel status`/`bskel next` built entirely on `collectGateStatuses()`/`checkArtifacts()` (the same primitives `verify` calls) plus S1's `GATE_NAMES` order and S2's `changed_inputs`/`stale_reason`, exactly the "unusually economical" case Codex's own P&L pass predicted. `blocked_by`/`next_actions`/per-action `mutating` all present in JSON; each gate's remediation matches its actual status (not_run/awaiting_disposition/stale get genuinely different commands, not a generic re-run). No dependency graph was built — `GATE_NAMES`'s documented order was sufficient, same reasoning S2 used to defer one. `--execute` deliberately not built (auto-running a mutating recommended command conflicts with this project's confirm-before-destructive-action default); spec-kit phases (4/6/9, no gate) are not tracked. See D-status-next in DECISIONS.md, including a real dead-code bug (a per-gate-loop special case that could never be reached given how the surrounding data flow actually works) caught by manually walking the CLI end to end, not by the test suite.]**

### D2. Stable CLI and diagnostic contract

- **What:** Replace `parseFlags()` and deep `process.exit()` calls with strict parsing and returned command results.
- **Why:** Unknown flags currently become positional arguments, missing values become `undefined`, and numeric values such as `--port`/`--max-behind` are not validated consistently. Errors are not reliably JSON even when `--json` is requested.
- **Scope/Effort:** **M**.
- **Concrete approach:** Use `node:util.parseArgs`, centralize exit-code definitions, and return `{ok, code, command, diagnostics, next_actions}` from handlers. Add global `--help`, `--version`, `--json`, and `--quiet`.
- **[IMPLEMENTED, `2cb0db4` — `node:util.parseArgs`-based strict parsing (`lib/cli.mjs`, one `COMMANDS` table mechanically transcribed from the 18 old `parseFlags()` call sites, proven lossless by a default-value snapshot test), a single exit-code table (`lib/exit-codes.mjs`; `lib/gates.mjs`'s `EXIT` now assembles from it and re-exports unchanged, no renumbering), global `--help`/`--version`/`--json`/`--quiet`, and an additive `sbf.cli-diagnostic/1` JSON envelope on `--json` for payload-less early-exit failures. Three real bugs found and fixed, all reproduced live: (1) `bskel verify --feature --json` silently swallowed `--json` as `--feature`'s value and crashed with an **uncaught Node stack trace**; (2) `bskel preflight --max-behind abc` made the underlying bash comparison fail silently (`set -euo pipefail` doesn't catch an error inside `[ ]`), **disabling the stale-base check this whole tool exists for** — demonstrated live by running the actual pre-fix script against a genuinely-stale worktree (reported `PASS` at exit 0; the fixed script rejects it at exit 14, closing the exact bypass this project's own founding bug (a 658-commit-stale worktree) belongs to); (3) `bskel status 001-widget` silently ignored a stray positional (a common `--feature`-typo shape) and reported success. The catalog's own "every handler returns a uniform envelope" prescription was rejected with direct evidence: `scan`/`contract emit`/`handles plan --json` each print a schema-validated artifact document (two `additionalProperties:false`) that `cmdScan` also writes byte-identical to disk — wrapping it would break `bskel scan --json > brownfield-scan.json`'s own schema validation — and 12+ existing tests already depend on non-zero exits carrying real payloads (verify exit 1, scan exit 16/3, handles emit exit 15, contract validate exit 1). The approved design adds the diagnostic envelope only to previously-empty-stdout failure paths, touching zero payload-bearing outputs. Exit-code renumbering was also rejected (the numbers are an existing public contract — SKILL.md + 11 tests assert them); exit 2's long-standing double meaning is disambiguated by a new `reason` string in the envelope instead. Two side fixes found during the audit: a numeric guard added directly to `scripts/preflight-base-ref.sh` itself, and a `{{PORT}}` substitution site materialized in `stack/bootstrap/ngrok.sh` (`--port` had **zero effect** previously — `stack/apply.mjs` already computed and passed the template variable, but the template never referenced it). Tests 386 → 444, all 386 pre-existing tests pass completely unmodified. See D-cli-contract in DECISIONS.md.]**
- **Status note (2026-08-16):** the process.exit() pipe-truncation audit (`3b5106a`) fixed the specific truncation bug in some exit paths but did not touch the broader "unknown flags silently become positional args" / "errors not reliably JSON" issues this item is actually about — resolved above.

### D3. Explainable scanner evidence

- **What:** Make every relevance point traceable. **Do not confuse with this repo's own "D3" in DECISIONS.md (`## D3: verdict -> disposition state machine instead of an agent question`) — a completely different, already-implemented concern that happens to share the same bare-number label. Same collision class as this catalog's own A2/A3 entries.**
- **Why:** `scoreModule()` returns only a scalar, uses symmetric substring matching, and allows repeated endpoint matches to inflate a module score. Reports contain file paths but no source spans or score breakdown.
- **Scope/Effort:** **M**.
- **Concrete approach:** Emit evidence records such as `{signal, term, value, weight, file, line}` and add `bskel scan explain <module>`. Tokenize identifiers and paths, cap repeated-signal contribution, and use deterministic tie-breaking.
- **[IMPLEMENTED as D3, `5234e5e` — followed the concrete approach as written, with two real deviations found during implementation, not planned in advance. (1) Capping had to move from "collect everything, filter at score time" to "cap DURING collection" after a real bug: an uncapped-collection first attempt made a 600-endpoint synthetic fixture's report balloon to 1.2MB, exceeding Node's default 1MB `execFileSync` buffer and crashing an existing regression test with a swallowed, invisible `ENOBUFS` (reproduced directly outside the test harness to confirm the exact cause). Capping at collection time keeps report size bounded regardless of repo size, and the `counted:boolean` per-entry field the original design planned became unnecessary — every recorded entry always counts. (2) Line-tracking was silently inconsistent across all 3 adapters before this item — `java-spring.mjs` (the primary adapter) had NONE at all; `python-fastapi.mjs`/`generic-grep.mjs` each had their own private, duplicated `lineNumberAt()`. Extracted to `scanners/text-util.mjs`, added missing line-tracking to controller/entity/enum extraction across adapters. `CAP_PER_SIGNAL=5` was determined empirically (swept 3/5/8 against the real Team-IZ-Backend oracle via `git stash` before/after comparison, confirmed every existing verdict — including `curriculum`'s score sitting exactly at `COLLISION_THRESHOLD` — survives unchanged at every value tested), not guessed. See D-scanner-evidence in DECISIONS.md.]**

### D4. Uniform plan/check/diff before writes

- **What:** Extend stack's dry-run ergonomics to every generator.
- **Why:** `handles plan` reports resources but not exact file actions or diffs, while `handles emit` writes immediately.
- **Scope/Effort:** **M**.
- **Concrete approach:** Standardize `--plan`, `--check`, `--diff`, and `--apply`; report `create`, `unchanged`, `update-generated`, and `conflict-user-edited`. `--check` should be CI-friendly and never write.
- **[IMPLEMENTED as D4, `60fa55e` — this catalog Why held up under grounding (unlike several prior items this session). The real gap was exposure, not computation: O2's `classifyFile()` already classifies every file into exactly the states this item asked for on every `handles emit` call — D4 exposes that classification via a dry-run path instead of reimplementing it. `emitUnits()` (`handles/_engine.mjs`) gained `dryRun`/`computeDiff` params (both default `false`, every existing call site including P4's conformance harness unaffected); a new `actions` array reports `{path, kind, action, resourceType?, diff?}` for every unit. `bskel handles plan --diff` now always previews the same per-file actions (diff bodies opt-in), reusing `provider.emit({dryRun:true})`. `bskel handles emit --check [--diff]` never calls `passNamedGate` and reaches the identical `EXIT_CODES.HANDLES_CONFLICT` (15) a real blocked emit would — no new exit code. Diff text is a real `git diff --no-index` (no new dependency — `git` was already a hard dependency of this same file). `--plan`/`--apply` flags from the catalog's literal wording were deliberately not added: `handles plan` already exists as a subcommand (would collide in name), and `handles emit`'s default (writes) was deliberately not flipped to dry-run-by-default like `stack apply` (would be a breaking change to an already-shipped command). Found live while grounding, not assumed: java-spring's `outputs.spec` file (`migration.sql`, the same P4-era quirk) always appears in `written[]` even when unchanged — the first `--check` implementation used `written.length` for its exit-code verdict and would have reported "changed" forever for java-spring; fixed by deriving the verdict from `actions` instead, confirmed correct against both real providers (java-spring end-to-end including a live `@PreAuthorize` diff, and python-fastapi's no-`outputs.spec` path). See D-handles-dryrun in DECISIONS.md.]**

### D5. Capability-aware doctor

- **What:** Make `doctor` workflow-specific.
- **Why:** `cmdDoctor()` always checks only `git`, `gh`, and `rg`; it misses Node compatibility, dependencies, Bash/curl/ngrok, Java/JDK, build wrappers, and adapter readiness.
- **Scope/Effort:** **S**.
- **Concrete approach:** Add `doctor --workflow scan|handles|stack --json`, distinguish required from optional checks, and include exact remediation.
- **[IMPLEMENTED as D5, `70c7721` — `bskel doctor [--workflow scan|handles|stack] [--json]`, new `lib/doctor.mjs::computeDoctorChecks()` (mirrors D1's `lib/workflow.mjs` split). Only `git`/a compatible Node runtime (checked against the real >=20.11.0 requirement `import.meta.dirname` needs, not package.json's inaccurate declared `>=18` -- see P1)/`rg` are `required`; `gh` (preflight's already-soft-guarded cross-check), build-wrapper presence (handles, reuses `lib/verify.mjs`'s newly-exported `detectBuildCommand()` — the exact function `verify --build` itself uses), and stack tooling are all optional with a remediation string. Stack tooling is sourced from a new optional `runtime.requires: string[]` field on `schemas/stack-choice.schema.json` (populated for `stack/catalog/ngrok.yml`: `[ngrok, curl]`) rather than hardcoded — a future catalog entry declares its own required binaries and doctor picks them up with zero code changes, the same declarative pattern D7/G1 established. Fixes a real over-strict bug: `gh` missing used to fail `bskel doctor`'s overall exit code even though nothing downstream actually breaks without it — now verified directly via a restricted-PATH test proving overall `ok` stays true. G1's adapter-readiness block is unchanged, just gated by workflow (shown for scan/handles/unscoped, not stack). Caught and fixed a real bug during implementation (not by the test suite): an early draft's `root ? a.detect(root) : null` collided with `detect()` itself legitimately returning `null` on a non-match, silently dropping java-spring's "-- does not detect this repo" line — fixed by coercing to `Boolean(...)`. Verified against Team-IZ-Backend: `--workflow handles` correctly detects the real `./gradlew`. Tests 337 → 345. See D-doctor-workflow in DECISIONS.md.]**

### D6. Feature lifecycle commands

- **What:** Add `feature list`, `show`, `rename`, `link`, and `archive`.
- **Why:** `feature init` is the only supported operation over `.sbf/feature-index.json`; manual editing is currently the documented recovery path for merges or re-keying. Auto-numbering also has a race between reading `specs/` and writing the new feature.
- **Scope/Effort:** **M**.
- **Concrete approach:** Treat the UID as primary, update index and feature paths transactionally, validate collisions, and preserve rename/link events in history.
- **[IMPLEMENTED as D6, `261e28a` — full scope: the race fix and all 5 lifecycle commands. Race fix: `feature init`'s whole read-`specs/`→compute-NNN→write→load-modify-save-index sequence now runs under one `withLockSync(root, 'feature-index', ...)` (reusing `lib/lock.mjs`, S5's exact pattern) — confirmed live with a real 20-process stress test: pre-fix, 20 concurrent same-slug `feature init` calls collapsed to 14 distinct feature_ids (6 silently overwritten); post-fix, 20/20 every time. `by_uid[uid]` was already an array (single-element, write-only) — D6 is its first real consumer: `feature rename` appends (the array IS the rename history), `feature link` records a separate `merged_into` cross-reference for the genuinely-different "two independently-created features, different uids" case, deliberately index-only (never an automatic scan/contract/handles state merge — same never-auto-resolve-ambiguity discipline as config patching). `feature archive` is a soft-delete (`archived_at` in place, no filesystem move). **Found live during implementation, not assumed correct in advance**: an early `rename` draft rewrote the `feature_id` field inside every `.json` under the renamed directory, including `contracts/<id>.schema.json`/`brownfield-scan.json` — these are hashed in FULL for gate tokens (`contract_hash`/`scan_report_hash`/etc.), so rewriting even one field silently invalidated an already-passed gate, confirmed by a real `handles emit --check` reporting a phantom stale contract gate immediately after rename. Fixed by leaving gate-token-hashed artifacts byte-identical (filename only); only `feature.json` and `.sbf/<id>.json` (never hashed as another gate's input) get their `feature_id` field rewritten. A second real bug: calling `fail()` (`process.exit()`) from inside a `withLockSync()` callback skips the lock's own `finally` cleanup, leaving a stale lock that hung every subsequent lifecycle command — fixed with a `LockedCommandFailure` throw-and-catch-outside-the-lock pattern. Two new schemas (`schemas/feature.schema.json`, `schemas/feature-index.schema.json` — neither file had one before). Verified against a real Team-IZ-Backend isolated worktree: renamed a real 15-operation organization contract, confirmed the gate token byte-identical before/after (not falsely stale) and the old id cleanly fails every command. Tests 594→612. See D-feature-lifecycle in DECISIONS.md.]**

## Gate/State mechanism

### S1. Declarative gate dependency graph

- **What:** Replace scattered recomputers and verifier lists with a single gate registry.
- **Why:** `GATE_RECOMPUTERS` and `GATE_SPECS` already disagree: `stack` is emitted and documented as optional verification state, but is absent from `verify`.
- **Scope/Effort:** **L**.
- **Concrete approach:** Define each gate's scope, upstream gates, required/optional policy, input resolver, declared outputs, freshness policy, and remediation in one module. Command execution, `require`, `verify`, `status`, and visualization should all consume that registry.
- **[IMPLEMENTED as S1, `18d0838`]**

### S2. Precise content manifests and partial invalidation

- **What:** Hash relevant inputs and outputs, not merely `HEAD`.
- **Why:** Uncommitted Java changes do not stale `scan`; unrelated commits stale every feature. `handles` hashes only the contract, and `stack` hashes only its record despite comments claiming applied-file drift is covered.
- **Scope/Effort:** **M–L**.
- **Concrete approach:** Store sorted manifests of repo-relative paths and hashes, adapter/template/tool versions, command options, and upstream gate tokens. Recompute only those files, report exactly which input changed, and invalidate downstream nodes transitively.
- **[IMPLEMENTED as S2, `374ec92` + `abee6f2` + `06dcc20` — a stale gate now reports exactly which input changed (`changed_inputs`/`stale_reason`) for all five gates. `stack`'s token hashes every applied file's content AND permission mode. `scan`'s token hashes a precise, live-re-derived fingerprint of the adapter's own real read-set (`adapter.listReadSet()`, never blind to a brand-new file, generated files excluded via O2's manifest). New `scan disposition --module <name>` (defaults to the same module `selectModule()` would also pick) persists which module a feature disposed onto, which `contract`'s token now uses to narrow to just that module's own `controllers`/`entities`/`enums` files (`scan_report_hash` + per-file `module_file:` hashes) — not the originally-sketched opaque `upstream_token` (that would have inherited scan's own whole-tree sensitivity, defeating the narrowing). `handles` needed no new tracking at all: its existing `contract_hash` input is already sufficient once `contract` itself is precise, so it just drops `head_sha`. `head_sha` is now gone from every gate except `preflight` (which needs it for a different, still-valid reason). All three original complaints ("uncommitted Java changes do not stale scan", "unrelated commits stale every feature", the `stack`/`migration.sql` drift blindness) are closed. Known, accepted, explicitly-named remaining gaps (not silently dropped): DTO file paths aren't tracked yet (no `.file` field in `scanJavaSpring()`'s DTO extraction), and a brand-new file added to an already-disposed module is only caught at the next explicit re-scan, not immediately. See D-gate-precision in DECISIONS.md.]**

### S3. Preflight freshness and failure semantics

- **What:** Make network freshness explicit and enforceable.
- **Why:** `preflight-base-ref.sh` ignores `git fetch` failure, while the stored token contains only HEAD and the local default-branch name. A preflight can therefore remain passed after the remote advances or after an unsuccessful refresh.
- **Scope/Effort:** **M**.
- **Concrete approach:** Record `origin_tip_sha`, fetch outcome, `checked_at`, dirty policy, and `max_behind`; fail closed on refresh failure unless `--offline` is explicit. Give online checks a TTL and let `require` detect movement of the local remote-tracking ref.
- **[IMPLEMENTED as S3, `761e092` — both halves shipped together (S3a+S3b). `git fetch` now fails closed at a new exit `18 REFRESH_FAILED` unless `--offline` (`--no-fetch` kept as an exact alias) is explicit; evidence gained `origin_tip_sha`, `checked_at`, `worktree_dirty` (now unconditional), `fetch` outcome, `policy`, and a per-source `cross_check`. The preflight gate's token gained `origin_tip_sha` (local remote-tracking movement detection, added alongside — never replacing — `head_sha`) and a `freshness: { defaultMaxAgeMinutes: 30 }` policy (data-derived from Team-IZ-Backend's real commit cadence — 5.1% chance the remote moves within 30 minutes), checked against the gate record's own `at` timestamp in `requireGate()`, independent of the token comparison. `--max-age-minutes`/`--fetch-timeout-seconds` exposed as flags. Re-estimated scope: code was M as catalogued, but behavior radius is L (an already-passed preflight can now expire from age alone) — presented to the user as 3 explicit decisions with real evidence before implementation; all resolved to the recommended option. See D-preflight-freshness in DECISIONS.md.]**

### S4. Append-only history and bounded overrides

- **What:** Keep gate events rather than only the latest record.
- **Why:** `setGate()` overwrites prior state, and a forced gate passes forever regardless of changed inputs.
- **Scope/Effort:** **M**.
- **Concrete approach:** Append pass, stale, disposition, force, and revoke events to per-feature JSONL; keep current state as a derived cache. Bind a force to the current input token and support expiry by time, commit, or next input change.
- **[IMPLEMENTED as S4, `3214dd3` — user approved the full scope (both the force-expiry bug fix and the JSONL history + revoke command) after the two halves were flagged as separable. Grounding found "expiry by commit" is mostly already implied by "expiry by next input change" — 4 of 5 gates already include `head_sha` in their own recomputed inputs (confirmed directly), so binding a force's token to real current inputs (not a synthetic `{forced,reason}` placeholder) catches a commit move automatically; `stack` is the one deliberate exception (`D-gate-precision`), not a gap. `requireGate()`'s `if (record.forced) return early` special case was deleted entirely — forced records now flow through the same staleness/freshness pipeline a real pass does, which turned out to be a net code SIMPLIFICATION, not just a fix. `checkFreshness()` gained one rule: a forced record never inherits the underlying gate's own TTL policy, only an explicit opt-in `--max-age-minutes`. History log deliberately excludes "stale" as a loggable event (it's derived at read time, never written — logging it would mean appending on every `verify` call). `revokeGate()`'s planned explicit `requireGate()` branch turned out to be dead code once written (the existing generic non-pass fallback already reports it correctly) and was dropped. Verified live: forced a real gate, landed a real new commit, confirmed it went STALE instead of staying pass-forced forever — the exact bug this item exists to close. See D-gate-history in DECISIONS.md.]**

### S5. Schema validation, migration, and concurrency

- **What:** Enforce the existing schemas at every persistence boundary.
- **Why:** `state.schema.json`, `scan-report.schema.json`, `feature-contract.schema.json`, and `stack-choice.schema.json` are never loaded. The current repo-scoped `_repo` state would itself violate `state.schema.json`'s feature-ID pattern.
- **Scope/Effort:** **M**.
- **Concrete approach:** Validate reads and writes, create a separate repo-state schema, add version migrations, and use lock files or compare-and-swap around load/modify/save so concurrent commands cannot lose updates.
- **[IMPLEMENTED as S5, `6f4d48a` — diverged from the concrete approach in three ways, each confirmed by direct execution before implementing rather than assumed: "separate repo-state schema" turned out unnecessary (S1 already made `_repo` a valid `state.schema.json` alternative, confirmed by ajv-validating a real generated `.sbf/_repo.json`); "stack-choice.schema.json...never loaded" is true but that schema validates `stack/catalog/*.yml` catalog entries (P4 territory), not `.sbf/stack.json`'s runtime record, which had no schema at all until this item added `schemas/stack-record.schema.json`; "version migrations" skipped entirely (every schema is still v1, nothing to migrate — building the machinery now would be designing for a hypothetical). `lib/schema-validate.mjs` wires validation into 5 real persistence boundaries (state/scan-report/feature-contract/contract-resolution/stack-record), consolidating 3 duplicated inline JSON-read call sites into one choke point along the way. `lib/lock.mjs` closes a real, confirmed-live lost-update race in `setGate()` — deliberately synchronous (an async design was rejected after tracing that it would force `async`/`await` through dozens of call sites up to `main()`), using `Atomics.wait` for a genuine blocking sleep and `fs.mkdirSync` for the mutual-exclusion primitive. A real tension with S3's deliberate `INVALID_TIMESTAMP` fail-closed design was found and resolved by loosening one schema field, not by weakening either feature. Verified with a 20-concurrent-process stress test against real `passGate()`: pre-fix code lost updates on every trial (17-18/20 survived), post-fix 20/20 every time. See D-persistence-integrity in DECISIONS.md.]**

### S6. Verification that cannot silently skip requested assurance

- **What:** Tighten artifacts and build verification.
- **Why:** `checkArtifacts()` checks little beyond the contract; generated handle outputs are not verified. `--build` still permits overall PASS when no build command is recognized, and the test named "reports a real build failure" actually exercises only the skipped-build branch.
- **Scope/Effort:** **S–M**.
- **Concrete approach:** Verify every gate's declared outputs and hashes, executable modes, resolver conflict state, and catalog artifacts. If the user explicitly asks for `--build`, unavailable build support should fail or require `--allow-skip-build`; capture both stdout and stderr.
- **[IMPLEMENTED as S6, `3183c84` — continuing `18d0838`'s migration.sql fix. `--build` now fails closed when no build tool is recognized (exit 1) unless the new `--allow-skip-build` flag is passed, exactly matching the catalog's own wording; a build failure's message now captures both stdout AND stderr (each its own last-30-lines window — confirmed live that a real failure's only useful diagnostic text can land entirely on stderr). The `stack` gate's staleness token now tracks each applied file's permission bits (a new, separate `applied_file_mode:<relpath>` input) alongside its content hash — confirmed live that stripping an applied script's executable bit left the gate `pass` forever before this. `bskel verify` now surfaces O2's real resolver-conflict detection (reusing D4's own dry-run `handles plan` call) as a new, non-blocking `conflicts` report — deliberately non-blocking: an early draft that blocked verify on this was a real false positive caught by an existing regression test, since a hand-finished `patchField()` is meant to look exactly like a "conflict" and must never fail verify (same class of trap S2's `D-gate-precision` already avoided for the gate token itself). "Catalog artifacts" re-scoped as not-a-gap: `bskel catalog lint` (P4) only ever validates YAML bundled inside the installed bskel package itself, never anything repo-specific a consumer's `verify` run could meaningfully check. See D-verify-integrity in DECISIONS.md.]**

## Distribution/Adoption

### P1. Publishable npm package and real README

- **What:** Package the CLI for reproducible installation.
- **Why:** `package.json` is private and the tracked repository has no README. It also claims Node `>=18`, while `contracts/validate.mjs` uses `import.meta.dirname`, which is not available across the declared Node 18 range.
- **Scope/Effort:** **M**.
- **Concrete approach:** Either replace `import.meta.dirname` with `fileURLToPath` or raise the engine floor; remove `private`, add a `files` allowlist covering templates/schemas/scripts, and test `npm pack` plus installation from the tarball. Add README quickstart, compatibility table, generated-file policy, security model, and troubleshooting.
- **[IMPLEMENTED as P1, `acc6804` — fixed the one `import.meta.dirname` outlier (`contracts/validate.mjs`, blast radius traced to exactly one subcommand, `bskel contract validate`) to match the portable pattern every other file already used, rather than raising the engine floor — grepped the whole runtime tree for every other recent-ES-addition pattern first and found nothing else needing more than Node 18. `lib/doctor.mjs`'s `MIN_NODE` corrected to match (`{20,11}` → `{18,0}`). `package.json`: `private` removed, `files` allowlist added (`npm pack` 96 files/1.15MB → 65 files/417KB, test/ fully excluded), `repository`/`homepage`/`bugs` filled in. Two new tests (`test/package-manifest.test.mjs`, `test/package-install.test.mjs` — the latter runs a real `npm pack` → scratch install → installed-binary execution, not just `node bin/bskel.mjs`). New `README.md` with all 5 requested sections, summarizing (not duplicating) existing DECISIONS.md/SKILL.md content. `SKILL.md` itself deliberately excluded from the npm package (user-confirmed) — pure Claude Code skill metadata `bskel` never reads at runtime. See D-npm-packaging in DECISIONS.md.]**

### P2. Repo-level `bskel init` and greenfield bootstrap

- **What:** Separate adopting bskel from creating a feature.
- **Why:** `feature init` assumes a prepared Git repo and later commands infer Spring conventions; there is no persistent project configuration or new-project path.
- **Scope/Effort:** **L**.
- **Concrete approach:** `bskel init` should detect/select adapters, write `.bskel/config.yml`, establish state/spec directories and ignore policy, and validate the default branch. A separate `bskel new --stack spring|fastapi` can create a minimal supported starter—preferably through pinned official generators—then run `init`, rather than turning every command into implicit framework scaffolding.
- **[IMPLEMENTED as P2, `af5a7a3` — re-scoped after re-verifying the Why against current reality (see D-greenfield-bootstrap in DECISIONS.md): `bskel doctor` already reports adapter detection/specificity, and `bskel preflight` already validates the default branch, so a persisted `.bskel/config.yml` was deliberately NOT built — it would be the only state in this tool trusted after being written once rather than re-derived fresh every run, opposite this project's own established anti-stale-state philosophy. The one genuine gap (no path from an empty repo to a scaffolded one) is closed by `bskel new --stack spring|fastapi` alone. `--stack spring` calls the real, pinned-dependency-set start.spring.io API (this tool's only network-calling command, `--offline` refuses cleanly); `--stack fastapi` ships a local template (no official FastAPI generator exists with comparable standing to Spring Initializr) matching the real scanner adapter's own detection requirements exactly. **Real finding from testing the full flow, not assumed**: `bskel preflight` requires an actual `origin` remote, which a fresh `git init` doesn't have — `bskel new` deliberately never auto-creates a remote or auto-chains into `preflight`, and prints the real required sequence instead (verified end-to-end with a real bare remote). Verified: a real `bskel new --stack spring` call against the live Initializr API produced a project that compiled clean via `./gradlew compileJava`; `bskel new --stack fastapi`'s output is recognized by the real `python-fastapi` adapter's own `detect()`. Tests 650→664. See D-greenfield-bootstrap in DECISIONS.md.]**

### P2b. Parameterized `bskel new` (greenfield project parameters)

- **What:** Let `bskel new` express the project being scaffolded, not just its name — group/package/artifact identity, Java version, packaging, dependency set on the Spring side; project metadata, `requires-python` floor, port, license and database driver on the FastAPI side.
- **Why:** P2 shipped `--stack` and `--slug` and froze everything else. `groupId` was always `com.example`, `javaVersion` always `17`, the dependency set always the same five ids, and the whole FastAPI template had exactly ONE substitutable token (`{{SLUG}}`) — `requires-python`, version, description, license and dependency list were frozen text. `bskel new`'s entire job is producing a project someone then owns, and "you may name it, and nothing else" makes it a demo rather than a tool.
- **Scope/Effort:** **M**.
- **Concrete approach:** Decide per parameter whether `bskel` can pass it through to Spring Initializr or must check it locally — by probing the real API, not by reasoning about it. Add local validators only where upstream fails OPEN, and validate the one moving-but-fail-open parameter against a live, never-cached metadata fetch. Extract the `{{VAR}}` renderer both halves already duplicated, and give the FastAPI template tree the residual-variable check `stack/catalog/` has had since P4.
- **[IMPLEMENTED as P2b, `8e60221` — the parameter split is measured, not chosen: a live validation matrix against `start.spring.io` (recorded verbatim in D-greenfield-parameters, with an explicit point-in-time caveat) showed `dependencies`/`type`/`packaging`/`language` fail CLOSED with a clean, quotable HTTP 400 message, while `groupId` (`com.new`, `has space`) and `javaVersion` (`99`) fail OPEN with HTTP 200 and produce projects that never compile. So: `--packaging`/`--dependencies` stay pass-through (and `scaffoldSpring` now surfaces Initializr's own 400 `message` verbatim, which is what makes pass-through honest rather than a shrug); `--group-id`/`--package-name`/`--artifact-id` get a real local validator (JLS 3.8 grammar + the complete JLS 3.9 reserved-word list — a language-specification constant, unlike a service's moving catalog); `--java-version` is checked against an **on-demand** fetch of `start.spring.io/metadata/client`, never cached and never persisted, and only when a non-default value is actually passed. **`--dependencies` REPLACES the baseline five** — a deliberate deviation from this item's own additive-only recommendation, chosen explicitly by the user — made safe by making the danger visible rather than preventing it: a resolved set missing `web`, `data-jpa` or `validation` prints one specific stderr warning per missing id naming the actual downstream consequence (no `@RestController` to scan / no `@Entity` for `resource.fetch` / A3's generated `patchField()` won't compile), then proceeds. `--add-dependencies` is the additive-only flag for the common case and never warns. **Three flags are refused with cited reasons rather than omitted**: `--type` (`detectJacksonPackage()` reads `build.gradle` only, so a Maven scaffold makes a later `handles emit` import a Jackson 2 class absent under Initializr's current Boot 4 default), `--language` (the java-spring adapter globs `*.java` only; a Kotlin scaffold falls through to `generic-grep`), `--boot-version` (HTTP 500 on a bad value + D-greenfield-bootstrap's standing maintenance argument). `baseDir` is never a flag AND never sent — the flat archive is what `unzip -d` and every adapter's `detect()` require — pinned by a regression test. FastAPI's half has no upstream authority at all, so `lib/template.mjs` (the `{{VAR}}` renderer extracted from `stack/apply.mjs` + `new/fastapi.mjs`, proved behavior-preserving by `test/stack-cli.test.mjs` passing 12/12 **completely unmodified**) now also fails the scaffold CLOSED if any rendered file still contains a `{{VAR}}`, rendering into memory first so nothing partial is ever written. `--database` pins a driver and generates no wiring, by design. **★ A real bug found by running it, not by review**: `--name "Demo Service"` wrote `name = "Demo Service"` into `pyproject.toml` — valid TOML, invalid project name, reproduced directly as pip's `configuration error: 'project.name' must be pep508-identifier`; nothing downstream of this tool checks a FastAPI project name, the same fail-open shape as `groupId`. Verified: defaults produce a byte-identical Initializr URL (query-parameter order included, pinned as a literal string); a real `bskel new --stack spring --group-id com.acme --add-dependencies actuator --packaging war` compiled clean via a real `./gradlew compileJava` (BUILD SUCCESSFUL, Spring Boot 4.1.1 that day); a real `pip install -e .` of the fully-parameterized FastAPI output reports `billing-svc 0.9.0 requires-python >=3.12 license MIT`; `detectPythonFastApiRoot()` re-asserted across 7 parameter combinations. No new CI job — the live-metadata path is tested against a mocked `fetch`, per D-greenfield-bootstrap's own "CI must not hit an external service" rule. Tests 771→819. See D-greenfield-parameters in DECISIONS.md.]**

### P3. Portable fixture corpus and CI/release pipeline

- **What:** Move real-world coverage into committed, anonymized fixtures.
- **Why:** The strongest Java scanner tests skip when `~/Desktop/Team-IZ-Backend` is unavailable, so CI would lose the main oracle. There is no tracked CI configuration.
- **Scope/Effort:** **M**.
- **Concrete approach:** Commit fixtures for the known Organization/Curriculum patterns, multiline annotations, security variants, multi-controller modules, malformed catalogs, gate invalidation, and codegen conflicts. Run Linux/macOS and supported Node versions, plus package-install and generated-Java compilation tests.
- **[IMPLEMENTED as P3, `04e3f5f` — Option B (CI pipeline + frozen clean-room-synthetic fixture corpus + a real generated-Java compilation check), selected over a smaller "CI + fixture corpus only" option after direct-execution evidence for each. Re-estimated M→L (six separable deliverables, same pattern as S3/G4). `test/fixtures/java-spring/` (scanned in place, never built) reproduces the Organization/Curriculum module oracles, all 3 `@PreAuthorize` branches, all 3 service-arity cases, and 6 scanner blind spots newly found by directly probing the regex (pinned as A2's future before/after baseline, not fixed here). `test/fixtures/java-compile/` (actually compiled) + `scripts/java-compile-smoke.mjs` automate the ONE thing this project had never automated: proof generated Java compiles — previously a one-time manual `./gradlew compileJava` run on one machine. `.github/workflows/ci.yml` (3 jobs, Linux-only, Node 22/24 — the documented 20.11.0 floor has a real, already-upstream-fixed Node core bug, confirmed live). The 16 tests gated on Team-IZ-Backend were rewritten as drift-resistant invariants, not deleted — the exact-count version broke in the field 4 days after being written (all 3 numbers moved). `test/package-manifest.test.mjs`/`test/ci-workflow.test.mjs` are the static safety nets `npm test` alone can't provide. macOS runner + a Python codegen import-check upgrade deferred to **P3b** below. See D-fixture-corpus in DECISIONS.md.]**

### P3b. macOS CI runner + Python codegen import-check upgrade

- **What:** Two small, independent P3 follow-ups, deliberately deferred out of P3's own scope.
- **Why:** Both are real improvements at real (if different) costs P3 chose not to commit to without a separate decision. A macOS Actions job — this repo is Private, and private-repo macOS runners bill at GitHub's published 10x multiplier versus Linux; this codebase has, to date, only ever run on macOS locally, so the marginal CI value is lower than Linux's (which P3 already added). Upgrading `test/python-fastapi-handles.test.mjs`'s `router.py`/`resolver.py` check from `ast.parse` (syntax-only) to a real `python3 -c "import ..."` (with a small committed FastAPI+SQLModel fixture skeleton) would catch real import/API-mismatch bugs syntax-checking structurally cannot, at marginal cost (`pip install fastapi sqlmodel`, no compiler toolchain) — `codec.py`/`registry.py` already have strong coverage via `test/handles-python-codec.test.mjs`'s real functional round-trip, so this is scoped narrowly to the two files that don't.
- **Scope/Effort:** **S** each.
- **Concrete approach:** Add a `macos-latest` job to `.github/workflows/ci.yml` (`main`-push + a weekly cron, single Node version, not every PR — confirm remaining Actions quota in the GitHub billing UI first). Separately, add a minimal `test/fixtures/python-fastapi/` skeleton (`pyproject.toml`, `app/api/deps.py`'s `SessionDep`, `app/models.py`'s entity + `<Entity>Public`) and swap the `ast.parse` check for a real import in a CI-installed virtualenv.
- **[Macos-runner half IMPLEMENTED, `fe15df1` — diverged from the concrete approach above: not `macos-latest` (GitHub-hosted, 10x-cost), but a self-hosted runner (`bob-macmini`) on the user's own idle Mac mini, proposed by the user once cost was explained. Self-hosted runners are free regardless of OS, which removed the ONLY reason this item's own text gave for restricting to "main-push + weekly cron, not every PR" — but an automated post-push security review caught that the job, as first written, still ran on `pull_request`, and self-hosted runners on `pull_request` execute the PR head's own code directly on the host machine (confirmed live: the vulnerable version's first run completed successfully on `bob-macmini` before the fix landed). Fixed with `if: github.event_name != 'pull_request'` before merge — so the job ended up restricted to push(main)/workflow_dispatch after all, for a different (security, not cost) reason than this item originally gave. See D-macos-runner in DECISIONS.md.]**
- **[Python codegen import-check half IMPLEMENTED, `277b8d9` — followed the concrete approach as written: `test/fixtures/python-fastapi/` (frozen, committed skeleton — `pyproject.toml`, `app/api/deps.py`'s `SessionDep`, `app/api/items.py`, `app/models.py`'s `Item`/`ItemPublic`) + `scripts/python-import-smoke.mjs` (new `python-import` CI job, Linux-only) replace, not duplicate, the old `ast.parse`-only e2e test — same "heavier check gets its own dedicated script + job, out of the fast default `npm test` path" precedent `java-compile-smoke.mjs` already established. Confirmed live, both directions, that this is a real strengthening and not busywork: deliberately breaking the fixture's import graph makes the new check fail with a real Python traceback that `ast.parse` would have missed entirely. See D-python-import-check in DECISIONS.md.]**

### P4. Extension conformance kit

- **What:** Provide supported interfaces for third-party scanners, code generators, and stack catalogs.
- **Why:** The catalog is described as generic, but entries are not schema-validated and templates can currently name unchecked relative paths.
- **Scope/Effort:** **M**.
- **Concrete approach:** Add `bskel catalog lint`, path-containment checks, declared variables, compatibility/version fields, fixture-based idempotence tests, and an adapter test harness. Keep extensions local/configured initially before designing a remote registry.
- **[IMPLEMENTED as P4, `aece607` — grounding found the catalog's stated Why already stale: `loadCatalogEntry()` already schema-validates every entry against `schemas/stack-choice.schema.json`, and `assertContained()` (D-security-4) already guards all 3 path sites inside `planApply()`. Implemented A+B+D+E, user-approved; **C (compatibility/version field) deliberately excluded from scope** — no current catalog entry or real compatibility-break case to size it against. A: `bskel catalog lint [<choice>] [--json]`, reusing `loadCatalogEntry()`+`planApply()` against a throwaway directory (no new validation logic). B: residual `{{VAR}}` detection via regex over `planApply()`'s rendered output, not a new declaration system (only one real consumer, `{{PORT}}`, existed). D: `test/stack-cli.test.mjs`'s idempotence test generalized to loop every catalog choice with a self-consistency assertion instead of a hardcoded ngrok file list. E: new `scanners/conformance.mjs`/`handles/conformance.mjs` — `schemas/handles-plan.schema.json`'s first real consumer anywhere in the codebase; dog-fooded against all 3 real adapters and both real providers (`test/conformance-harness.test.mjs`), which caught a genuine, pre-existing design wrinkle in java-spring's `emit()` (spec-owned files like `migration.sql` are deliberately regenerated every call) that the harness had to account for via the provider's own declared `outputs.spec` field rather than a hardcoded exception. See D-extension-conformance in DECISIONS.md.]**

## Other

### O1. Enforce the secret-file boundary

- **What:** Remove `.env` reads from code-generation commands.
- **Why:** `planApply()` reads the entire target `.env` to detect keys, contradicting both the target-repo instruction and the generated script's claim that only the human-invoked runtime step touches `.env`.
- **Scope/Effort:** **S**.
- **Concrete approach:** Detect static application from generated files and `.env.example`; report runtime secret configuration as `unknown`. If runtime checking is needed, put it in the human-run bootstrap script and output presence-only status.
- **[ALREADY IMPLEMENTED, independently of this catalog — DECISIONS.md's security hardening pass item #6, `839259c`/`bcdc427`. `planApply()`'s `alreadyDetected` is now decided from `detect.files` alone, never reads `.env`; `test/stack-cli.test.mjs`'s "stack apply dry-run does not read .env" regression test covers exactly this. Flagged by Codex during the 2026-08-16 P&L pass, confirmed by grep against both files.]**

### O2. Conflict-safe generated-file ownership

- **What:** Prevent regeneration from overwriting human work or another feature's shared artifacts.
- **Why:** `emitHandles()` writes global infrastructure and resolver stubs unconditionally. A second feature can overwrite shared files, and rerunning the first can erase a completed `patchField()`.
- **Scope/Effort:** **M**.
- **Concrete approach:** Maintain an artifact manifest with owner, template version, generated hash, and last-seen hash. Treat global infrastructure as repo-owned, resolvers as resource-owned, create resolver stubs once, and refuse to overwrite diverged files unless a three-way merge or explicit approval succeeds.
- **[IMPLEMENTED as O2, `0d1546f` — safety is content-derived, not manifest-derived: new `.sbf/handles-manifest.json` (`lib/handles-manifest.mjs`) records the hash of what `handles emit` actually wrote per file, so a rerun can tell "untouched since generation" (safe to regenerate) from "diverged" (refuse) — but the manifest's ABSENCE (fresh checkout, CI, a repo not committing `.sbf/`) is never treated as evidence of safety: `classifyFile()`'s fallback compares disk content against a pristine re-render, and a mismatch there can only ever produce a conflict, never a silent overwrite. Infra (`global/handle/*.java`) is repo-owned/all-or-nothing; resolvers are resource-owned/independent per file (one hand-finished `patchField()` never blocks generating a different resource's resolver). "Regenerate when provably untouched, not create-once" (Codex's original suggestion, rejected) — `requiredAuthority()` is re-derived live from `@PreAuthorize` every run (D-security-7), and "once" would let an enforced role silently go stale after a legitimate authorization change. New `--force --reason "..."` overwrites only genuinely diverged files, refuses on an uncommitted working tree (so the overwrite stays recoverable from git history), and a resolver the current plan no longer generates is reported as an orphan, never deleted. New exit code 15. **Two real bugs found live**: every resolver template bakes in `{{FEATURE_ID}}` (the only one of 9 templates that does), so a naive adoption check would false-CONFLICT every second feature touching an already-generated resource type — fixed by recovering the original owner from the resolver's own javadoc marker and re-rendering with THAT id before comparing; and a real `--force` overwrite against Team-IZ-Backend was misreported as "had no effect: 0 conflicts" (a successfully force-resolved conflict is removed from `conflicts`, so the original `conflicts.length === 0` check was also true right after an effectful run) — fixed with a dedicated `forced` array, locked in with a regression test. The `handles` gate token is deliberately left unchanged (still just `head_sha` + `contract_hash`) — hashing generated content into it would make every intentional `patchField()` edit report the gate stale, backwards for a workflow where hand-editing that method is the point. See D-handles-ownership in DECISIONS.md.]**

### O3. Define and enforce the handle trust model

- **What:** Decide whether handles are registered identifiers or freely forgeable self-describing IDs, then make the code consistent.
- **Why:** The generated `fetch()` and `patch()` paths decode type/UUID/pointer but never query `HandleRegistry`, so nonexistent or revoked handles still operate. Resource `handle_uid` also equals the resource UUID regardless of type, permitting cross-type registry collisions for identical UUID values.
- **Scope/Effort:** **L**, potentially breaking.
- **Concrete approach:** Prefer registry enforcement because registry/revocation already exist: fetch the derived UID, require a non-revoked row, and compare every decoded component. For a versioned `sbf2` format, derive all UIDs from `kind:type:uuid:pointer` so type participates in identity; provide migration/dual-read support.
- **Status note**: this repo's history already tracked a conditional version of this exact warning ("O3: reverses to top priority if handles are ever deployed to production") and confirmed via `git grep`/GitHub code search across all branches that handles have never been deployed to Team-IZ-Backend -- still deferred on that basis, unchanged by this recovery.

### O4. Complete the handle read and snapshot lifecycle

- **What:** Add field reads, mint/register operations, snapshot recording, and real current-contract drift checks.
- **Why:** `HandleController.java.tmpl` always returns 501 for field fetches. No code calls `HandleRegistry.create()` or `HandleSnapshot.create()`, and `HandleSnapshot.java.tmpl` references a nonexistent `HandleAspect`.
- **Scope/Effort:** **L**.
- **Concrete approach:** Add an explicit handle service for mint/register/revoke, resolve approved JSON Pointers through Jackson `JsonNode`, and record snapshots through an opt-in interceptor with redaction and retention rules. Query the latest eligible snapshot directly in the repository and compare its hash with a real current-contract hash provider—not an unsynchronized registry string.
- **[IMPLEMENTED as O4, `a816312` — user-approved full scope (AskUserQuestion), including the opt-in AOP auto-interceptor. `ResourceResolver` gained `contractRef()`/`featureUid()` (baked-in constants at `handles emit` time, same mechanism as A5's `contract_hash`); new `HandleService` (explicit `register`/upsert, `recordSnapshot`, `revoke`, `pruneSnapshotsOlderThan` — never auto-invoked, retention never auto-scheduled) finally makes `HandleController.recover()` reachable; field-level `fetch` now walks `JsonNode#at` instead of always 501ing; new `@RecordHandleSnapshot` + `HandleAspect` give an opt-in AOP path (explicit `resourceUidParam`/`redact`, never inferred), requiring `spring-boot-starter-aop` on the target's own classpath (never auto-added). Two real, previously-undiscovered bugs found by this item's own real integration test and fixed: (1) A3's generated `patchField()` validated the WHOLE reconstructed DTO (`validator.validate`) instead of just the patched property, so any field with an `@NotNull`/primitive sibling always 400'd — fixed with `validator.validateProperty`; (2) `HandleController.recover()` double-JSON-encoded its `payload` field — fixed with `objectMapper.readTree`. Verified against a genuinely running `@SpringBootTest` app and a real disposable Postgres (new `java-integration` CI job, `scripts/java-integration-smoke.mjs`): 4/4 real HTTP integration tests green (full lifecycle, schema_drift, field-fetch, persistence-layer redaction). See D-handle-lifecycle in DECISIONS.md.]**

### O5. Replace role-string inference with authorization contracts

- **What:** Make resolver authorization action- and resource-aware.
- **Why:** `findRequiredAuthority()` recognizes only an exact class-level `hasRole('…')` expression and otherwise emits `"TODO_ROLE"`. It cannot preserve method-level checks, `hasAuthority`, multiple roles, ownership, organization membership, or service-layer policy.
- **Scope/Effort:** **L**.
- **Concrete approach:** Replace `requiredAuthority()` with a fail-closed `authorize(authentication, action, resourceUid, pointer)` contract. Generate only policies that an adapter can prove equivalent; otherwise require an explicit policy implementation before the controller bean is enabled. Test anonymous authentication, tenant isolation, revoked handles, and arbitrary-pointer attempts.
- **Status note**: same deferred-with-O3 status as above (this repo's history calls this pairing "O3/O5 보류").

### O6. Deterministic artifacts and documentation integrity

- **What:** Remove avoidable output churn and automatically detect stale documentation references.
- **Why:** Contracts contain `generated_at`, discovery order is not consistently sorted, and `detectBasePackage()` chooses the first discovered application class. Documentation/code also reference missing `D-db`, `D8`, and `HandleAspect`.
- **Scope/Effort:** **S–M**.
- **Concrete approach:** Keep timestamps in gate history rather than semantic artifacts, sort all discoveries, fail on ambiguous application roots, and add a test that validates decision anchors, documented commands/gates, generated-template references, and CLI help against implementation.
- **[IMPLEMENTED as O6, `7d31345` — `generated_at` removed from `contract emit`/OpenAPI-snapshot output (and the now-stricter feature-contract schema); every `rg --files` call site sorted (`scanners/adapters/java-spring.mjs`, `scanners/adapters/generic-grep.mjs`, `handles/emit.mjs`), plus a `.localeCompare()` tie-breaker for `scanners/index.mjs`'s score sort; `detectBasePackage()` now throws a named-candidates error on a genuinely ambiguous multi-application repo instead of silently picking `files[0]` (same-package multi-file repos, a real multi-module shape, still resolve quietly), with both `bin/bskel.mjs` call sites routed through a shared `detectBasePackageOrExit()`. All three dead documentation references this item names resolved to real anchors. New `test/doc-integrity.test.mjs` makes that class of drift a test failure going forward, including a self-verification subtest proving the checker can actually fail, and specifically models the `D-security-N` numbered-list convention that would otherwise produce ~10 false positives. Verified against Team-IZ-Backend in an isolated worktree: `scan`/`contract emit` produce byte-identical output (including the on-disk contract file) across two consecutive runs, and the emitted contract file no longer contains `generated_at`. No override flag for the ambiguous-base-package case was added speculatively — no real multi-application repo has been observed in this project's testing. See D-artifact-determinism in DECISIONS.md.]**

## Notes on DECISIONS.md gaps (Codex's own free-form notes, verbatim)

**OpenAPI bridge / drift detection.** Close this with a normalized operation IR shared by source scanners and OpenAPI, not a one-off spec diff. Support committed files and explicitly requested live URLs, retain both provenances, and require disposition only for semantic conflicts; this simultaneously closes endpoint-correlation, typed request, response/error, and cross-framework gaps.

**DB-schema scan plane.** Start with migration files, then add an explicitly credentialed, read-only PostgreSQL adapter that receives the name of an environment variable rather than opening `.env`. Diff declared migrations, scanned entities, and live catalog state as three separate sources so "code differs from DB" does not get collapsed into one ambiguous warning.

**Comment-preserving config patching.** Replace catalog regex notes with typed patch recipes: target file, semantic YAML path, expected old-state predicate, desired value, and postcondition. Use `yaml`'s Document API only when the predicate matches exactly, show a diff first, and refuse aliases, multi-document YAML, duplicate keys, or ambiguous paths until separately supported.

**Live DB-backed handle round trip.** The first closing step need not touch Supabase: run the emitted migration and generated repository/controller behavior against disposable Testcontainers PostgreSQL. Keep a separate opt-in Supabase compatibility test for RLS/extensions; this provides repeatable mint→fetch→patch→recover coverage without shared credentials or infrastructure mutation.

**`patchField()` generation.** Use an approved `PatchStrategy` manifest rather than direct inference-to-code. The analyzer can propose one of the three observed conventions per pointer, show the DTO/service evidence, and generate only after approval plus a compilation/unit-test check; ambiguous fields remain explicit stubs.

**Additional stack entries.** Build a catalog conformance suite before adding Supabase or Railway: validate schemas, path containment, idempotence, required tools, secrets handling, readiness probes, cleanup, and rollback behavior. Then add one database-oriented entry to pressure-test whether the current static/runtime split is expressive enough before introducing a `custom` escape hatch.

**Response/error envelopes.** Seed response schemas primarily from OpenAPI, then enrich errors from Spring `@ControllerAdvice`/`@ExceptionHandler` mappings and representative golden payloads. Validate status code plus media type plus payload schema, while allowing an explicit `unknown-response` waiver when no trustworthy shape exists.

**Status note (2026-08-16)**: this note has since been substantially addressed by A2+A3 as actually implemented (response schemas seeded from OpenAPI, `anyOf` unions, fail-closed on unresolved schemas) -- though not the `@ControllerAdvice` error-enrichment half, which remains open.
